# 300363.SZ (博腾股份) — full-chain case study — 2026-08-08

**Charter:** ANTICIPATION §6.8(f)(1) — the operator's flagship CN winner (Prophet CN #1
2026-08-05 → ChiNext +20% limit 2026-08-07 → Mastermind Portfolio AI reportedly added at
17.03 and sold next day). Receipts-level trace by an Opus review lane; extends #4877 (bucket
un-publish census) and #4964 (PIT latch); resolves the parity-anatomy discrepancy. Read-only
forensic; heal lanes chartered separately.

## The chain (all stores under this repo root)

| When | Artifact | Record |
|---|---|---|
| 07-30…08-04 | `data/china_prophet_rank/candidates.parquet` | **No row.** Universe ~1470; jumps +198 to 1667 on 08-05 — the name entered the scored universe the same session it ranked #1 |
| 08-04 | `data/china_stocks/300363.SZ.parquet` | Ignition bar 15.61→17.73 (+13.6%), vol 51.2M vs 11.9M prior |
| 08-05 03:27 PT run 1 (`b62c157c57e`) | shadow parquet | `not_raw_eligible`, rank 367, score 44.07, `gate_reason "buy blocked by filter: counter-trend, no 200-reclaim/hold"`, `await_confluence`; organs already lit: `washout_2w=True`, `reversal_member=True (27/150)`, off_high −39.9%, potential 85 primed, residual_alpha 0.64. Board at this rev: 15 rows, name ABSENT, zero `reversal_member==1.0` rows |
| 08-05 06:59 PT run 2 (`ba1f127e4e9`) | `data/china_standout_track/board.parquet` | `board_rank 1`, T2, `ticks 0.0`, featured, score **90.32**, `entry_status partial`, stage ENTRY, `species cn_washout`; the run adds exactly 2 rows and both are the board's only `reversal_member==1.0` rows |
| 08-05 | `china_standouts.json buy[0]` | conviction 86 high / Primed 蓄势待发, size `{half, 50%, capped_by_entry}`, verdict "Reversal buy — washed out and turning up" 反转买点, rank_pctile 95 |
| 08-06 (`8e8ce5ec7b4`) | published board | **Erased from every live lane** (only track/reversal ledger rows remain). Close 17.03 |
| 08-07 | price store | 20.44 = 17.03 × 1.2002 — ChiNext limit close, vol 73.2M. Shadow: rank 1557, `reversal_member=False (95/161)` — the cohort dropped it after the move |

## Why #1 — exact decomposition (v2 weights recovered by arithmetic)

`90.32 = signal 35 (1.0×35) + entry 22.5 (0.9×25, partial) + runway 18.824 + bottom_quality 4
+ reversal_member 10`. Margin over runner-up 688306.SS (85.22) = **+5.11, of which
reversal_member is +10.00** — remove the channel and the name ranks **4th**. `theme_timing`
did not exist on the signal night (v2 board); under v3 it back-computes to 3.75/15 — a DRAG,
below the 0.6 neutral floor. **v3's own re-ordering (partial 0.9→0.6) would NOT reproduce
this rank.** Cohort scarcity was load-bearing: reversal flag on 2/17 rows; washout on 6/17.

## The execution leg — NO RECEIPT EXISTS

- `engine/neuralweb/market_packet.py:950-993` (the chat/bot grounding path) extracts ONLY
  counts from the board — no per-ticker rows, no conviction, no size tier. It cannot supply
  "86 / BOTTOMING / 半仓".
- Every phrase in the operator's quoted rationale is a **published board field** (conviction
  86, potential.reasoning[0] BOTTOMING, entry_urgency now, residual_alpha 0.64
  zero-authority, size half + `entry_signal.py:37` 部分入场), not a bot rule.
- **No holdings/positions/trade artifact exists in either repo** (PortfolioView.tsx is a
  104-line display; no Supabase positions; data/ai_desk, data/master_brain carry no 300363).
- Timing the artifacts force: 17.03 is the **08-06 close — the session the name was erased
  from every board lane**; the +20% close was itself a locked limit (fillable=... the fire
  receipt says `limit_state "open", fillable true` at FIRE on 08-05 @17.6 — the WIN day
  close was not fillable). The add and the sell are unreproducible from artifacts.

## Discrepancy RESOLVED — not a signal-date skew

The 08-05 shadow row is byte-stable across all four nightly commits, `signal_asof ==
signal_bar_asof == 2026-08-05` — no repaint, no skew. **Cause: the asia lane ran TWICE on
08-05 and both stores are keep-first — they latched DIFFERENT runs.** candidates.parquet
kept run 1 (not eligible); board.parquet first saw the name in run 2 (#1). Decomposition
exact: signal 35.00 + entry 11.25 = 46.25 = 90.32 − 44.07. Side effects: (1) board.parquet
holds **two board_rank==1 rows for (08-05, v2)** and ranks 2-6 are run-1 ranks — **the
stored 08-05 rank vector is a board that never published** (major); (2) the reversal-cohort
join existed only in run 2 — the organ that made the #1 was absent from run 1 (major).

## Defects filed

- **BLOCKER — published forward-ledger entry price is mutable and was mutated.** At
  `8e8ce5ec7b4` the row read `e=16.30, l=17.03, p=+4.5%`; today `e=17.52, l=20.44, p=+16.7%`.
  `e` is the T+1 open re-derived nightly, and the 08-06 bar as stored that night had
  **open 16.2999 < low 16.98** — an impossible bar, no open∈[low,high] sanity check on the
  path. The same corrupt-open shape is LIVE on the 08-07 bar right now. Published returns
  restate silently when a bad bar heals.
- **major** — `fill_basis "t1_hl2"` (`china_standout_track.py:101`) was used in NEITHER
  epoch (HL2 of 08-06 = 17.38; ledger used 16.30 then 17.52). Provenance string false.
- **major** — `china_prophet_shadow.py:41-47 SCORE_COMPONENTS` never gained theme_timing;
  v3 rows' stored components do not sum to prophet_score (residual 3.746 here).
- **minor** — parity anatomy's `rev_z 2.17` was `legacy_setup`; true `rev_z 0.6718`
  (corrected in the parity receipt this date). Conviction 85 (shadow) vs 86 (card).
  `track_ledger.as_of` stale vs its own rows.

## Replication conditions (what a US/HK port must wire)

1. Reversal-cohort rank channel, binary membership, full 10/100 weight — NECESSARY here.
2. Cohort scarcity (2/17) — a channel that lights broadly cannot separate.
3. Deep-base precondition (washout_2w + off_high ≤ −40% + below-200d).
4. T2 at `ticks=0` surviving to publication (#4964 PIT latch).
5. Patience/partial entry band VALUED (v2 0.9; v3's 0.6 forfeits 6 pts on identical inputs).
6. Same-session universe admission (the name entered the universe on its signal day).
7. **One run per stamp_date, or run_id on every keep-first store** — else ranks are
   unreconstructible (this case).
8. PIT-latched entry price + open∈[low,high] bar sanity — else published returns mutate.
9. Per-ticker board detail in any bot grounding path + a decision ledger — else execution
   legs have no receipts.
10. Fill reality modeled: the winning close was a locked limit — unfillable when earned.
