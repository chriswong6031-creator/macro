# Build Sequence

## K3E-0

Records-only architecture freeze.

Exit condition:

- owner law frozen;
- no-rebuild law explicit;
- `SRC-A1`, `VEND-0`, `EVAL-0` handoffs written;
- Agent OS validation clean.

## Parallel wave set after K3E-0 acceptance

### `SRC-A1`

Primary dependency for expectation truth. Preserves raw prospective estimate
observations in the existing revisions owner lane.

### `VEND-0`

Runs independently. It informs whether later expectation depth should remain
free-estate only or become a vendor-enabled program.

### `EVAL-0`

Runs independently. It prevents later model shopping.

## Gated downstream sequence

1. `EXP-1` expectation surface over lawful accrual
2. `MKT-1` market-response surface over reused price / residual / options planes
3. `CPL-1` coupling / lag / disagreement emission
4. `PHASE-1` descriptive phase projection

No downstream wave may quietly absorb another:

- `EXP-1` does not do vendor procurement.
- `MKT-1` does not build a residual engine.
- `CPL-1` does not mint a score.
- `PHASE-1` does not promote predictive authority.
