# Mastermind Alpha Intelligence Expansion — Wave-c0g Adjudication (GROK-G0 return)

**Date:** 2026-08-19 (UTC) · **Seat:** Fable Program Integration COO (FABLE-00, wave c0g) · **Session:** `claude/china-alpha-fable00`
**Baseline:** c0 packet `research/alpha_intelligence/C0_WAVE0_ADJUDICATION_2026-08-19.md` (#5933) · **This adjudication's pin:** `origin/main` @ `aa9ee6cd3f68`
**Authority of this document:** NONE. Dated adjudication snapshot under `WS:ALPHA-INTELLIGENCE-INTEGRATION` (runtime authority NONE, permanently).

This wave closes Wave-0: the sixth and final census (GROK-G0, post-event
reinterpretation) is returned in the same PR as this packet
(`research/alpha_intelligence/censuses/G0/`, six files) and adjudicated below.
Wave-0 is now **6/6 returned, 6/6 adjudicated**.

---

## 1. Execution deviation, declared

The G0 commission file existed in the operator's fanout pack but the Grok lane
was never dispatched. Rather than leaving the estate's architecture freeze
(and the China program's §17 stop condition) blocked on an external dispatch,
FABLE-00 commissioned a macro-fleet sonnet researcher to execute the census
read-only under the identical commission text + PASS-0 §6 G0 rider. Every
artifact header declares this. The commission's substance (read-only audit,
claim tags, casebook honesty, no builds) is model-agnostic; nothing in the
return depends on which fleet executed it.

## 2. G0 adjudication — **ACCEPTED, rider-compliant, with declared shortfalls**

**Rider compliance:** the deliverable ADDRESSES the Earnings owner
(`WS:EARNINGS-INTELLIGENCE-OS` named in every file; the E2 freeze is read and
respected — no scope change proposed; the census's own no-build warnings bar
an independent G lane). The frontier spec is explicitly DRAFT/non-authoritative;
the reaction-geometry file is an input census with no score proposal. Compliant.

**Casebook shortfall ruling:** 48 rows delivered against the 60-row floor —
29 rows on 18 PRIMARY SOURCE VERIFIED distinct events (full citations), 19
rows on 13 INFERRED pattern-only events with no fabricated magnitudes; all ten
commissioned classes carry ≥2 rows; classes 1 (negative gap/full recovery) and
10-short-window (reaction rejected) are weakest. **Accepted as returned.** Per
epistemics law a research-tier shortfall never blocks display-tier work, the
shortfall is declared rather than padded (exactly the honesty the commission
demanded), and any actual G build re-derives its casebook against repo-native
events under the owner's own E-wave anyway — the casebook's architectural job
(showing WHICH frontier states carry the incremental information) is met.

**Findings adopted as binding context for any future G-wave:**

1. **The eight-state frontier vocabulary does not exist and must be a DERIVED
   READ-ONLY VIEW.** `engine/company_intelligence/events.py:44-69` closes
   `EVENT_STATES` with an explicit transition table; adding frontier states to
   that enum is an authority-changing edit. The lawful construction computes
   frontier timestamps from existing fields (`event_workspace.v1` lifecycle
   pairs, `ReleaseRevision.acceptance_datetime`, filing acceptance, analyst
   snapshot `as_of`) without writing into the state machine. This is the
   owner's first adjudication (G0 Q1/Q2), pre-framed, not pre-decided.
2. **Beat/miss is legally fenced.** `event_workspace.py:272-278` raises on any
   beat/miss delta absent a licensed `basis_match`; `promotion.py:52` bans
   `market_reaction`/`market_data`/`trading_action` as promotion inputs;
   `digest.py:397-400` forces `market_reaction: not_joined`. Any G-wave
   reaction-geometry build is therefore blocked on an AUTHORITY decision
   (G0 Q3/Q4), not a coding task. No wave may route around this by computing
   reaction outside the claim graph and joining it back informally.
3. **The estate's one live earnings-reaction computation is reconnectable, not
   rebuildable.** `engine/expectation_state.py` (PEAD/SUE, display-only,
   PIT-disciplined) is structurally disconnected from the Earnings claim
   graph. A future G-wave evaluates RECONNECTION (G0 Q8) before any third
   plane is built.
4. **Two clock-pair precedents, zero inheritable code.** The
   source/recorded-clock discipline exists twice (`earnings_release/binding.py`
   acceptance clock; FIF's bitemporal cutoff pair) but reaches neither the
   transcript path, `qa_exchanges`, nor the analyst collector. A frontier spec
   imitates these patterns; it does not get them for free.
5. **PIT coverage boundaries stand:** ANALYST_REVISION_STATE is only
   forward-accruing (yfinance snapshot; no historical vendor panel);
   HEADLINE_AVAILABLE is modeled nowhere; FIRST_SESSION_CLOSE has no live
   computation. Historical G studies respect these as coverage boundaries —
   never backfilled, never zero-filled.
6. **Options-reaction is UNKNOWN, not NOT_BUILT** — the ~20 `engine/options_*`
   modules were census'd by existence and targeted grep only (G0 Q5 names the
   bounded follow-up read). The distinction is preserved deliberately.

**Routing unchanged:** any G build is an Earnings-OS wave adjudicated by that
owner, queued behind E2 (PASS-0 §1-G; c0 lane table). The eight owner
questions (G0_OPEN_QUESTIONS_US_ESTATE.md Q1–Q8) transfer to `WS:EARNINGS-INTELLIGENCE-OS`
at whatever wave boundary the owner next opens; nothing dispatches now.

## 2-bis. Dual G0 returns — adjudicated as COMPLEMENTARY, both accepted

While this packet was being assembled, a second, independent G0 return
appeared: **PR #5943** ("G0 China post-event reinterpretation census"),
China-scoped, also addressed to `WS:EARNINGS-INTELLIGENCE-OS`, landing seven
files in the same `research/alpha_intelligence/censuses/G0/` directory. The
G0 commission slot therefore received TWO executions with different readings
of scope: this PR's return audits the US/estate earnings machinery the
commission's AUDIT list names (event_workspace, FIF, releases, transcripts,
analyst revisions, reaction); #5943 censuses the China program's post-event
tapes (业绩预告/业绩快报 vintages, CNInfo inquiry/reply, 互动易 keep-LAST Q&A,
sell-side tape) and finds the missing piece is a `canonical_event_id` join —
`event_workspace.v1` cannot admit A-share issuers because `company_id` is
CIK-only.

**Ruling:** both returns are ACCEPTED; neither supersedes the other — they
cover disjoint halves of the same owner's territory and BOTH route inside
`WS:EARNINGS-INTELLIGENCE-OS` behind E2. To keep the shared directory
conflict-free this PR's return-packet file is named
`G0_OPEN_QUESTIONS_US_ESTATE.md` (renamed from the commissioned
`G0_OPEN_QUESTIONS.md`, which #5943 also emits — filename yielded to the
China-scoped return; the commissioned content is unchanged). #5943's central
identity finding (CIK-only `company_id`; 快报 collected-and-unused) is adopted
as China-program context feeding PR-0D (exact identity extension) and the F3
family design in `research/CHINA_ALPHA_INTELLIGENCE_MASTERPLAN.md`. #5943's
own AgentOS edits (Earnings WS/handoff) are that PR's cargo, adjudicated by
the Earnings owner — not re-ruled here. Wave-0 remains **6/6**; the slot is
simply double-covered.

## 3. Estate state after c0g

| Item | State |
|---|---|
| Wave-0 censuses | **6/6 returned, 6/6 adjudicated** (A0/B0/D0/E0/F0 in c0 #5933; G0 here) |
| Wave c0g (`WS:ALPHA-INTELLIGENCE-INTEGRATION`) | **done** (this PR updates the record) |
| FABLE-A | dispatched separately under `DEC:ALPHA-INTEL-FABLE-A-CONTRACT-FIRST-DISPATCH` (unchanged by this wave) |
| G lane | WAIT, inside the Earnings owner behind E2 (unchanged) |
| China program | executes under `WS:CHINA-ALPHA-INTELLIGENCE` per `research/CHINA_ALPHA_INTELLIGENCE_MASTERPLAN.md` (this PR) |

## 4. Evidence trail

- G0 return packet: `research/alpha_intelligence/censuses/G0/G0_OPEN_QUESTIONS_US_ESTATE.md`
  (closing RETURN sections) + six files, line counts 244/235/95/73/184/229.
- Strongest receipts re-read at adjudication: `event_workspace.py:272-278`,
  `promotion.py:52`, `digest.py:397-400`, `events.py:44-69`;
  FIF-1R3 freeze commits `ef2554c9909f` / `e2a584496b08`.
- Casebook coverage table: `G0_POST_EVENT_CASEBOOK.md` §1 (per-class rows,
  18 verified events with citations).
