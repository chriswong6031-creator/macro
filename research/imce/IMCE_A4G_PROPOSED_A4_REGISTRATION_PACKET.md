# IMCE-A4G / A4P — Proposed A4 Registration Packet

**STATUS: PROPOSED — NOT REGISTERED.** No `data/` write has occurred. No `declared_budget` row has actually been appended to `data/trial_ledger.jsonl`. No `rf.cycle_pattern.imce_*` family is registered by this document or by any document in the A4G/A4P wave sequence. Registration is a separate, future act (A4 / IMCE-03), requiring its own wave approval per `agentos/workstreams/WS-CYCLE-PATTERN-ISSUER-MECHANISM.md`.

**Wave:** A4G, regenerated by A4P (ruling 7, 2026-08-21). Records-only. **This document proposes exact byte-ready row contents; it registers nothing.**
**Authority:** amended contract V1.2 (`IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md`), as amended by `IMCE_A4G_AMENDMENT_LOG.md` (AG1–AG18, AP1–AP7); contract §1 (cell budgets, A5), §11 (multiplicity and trial budget), §15/§15a (stop condition, two-commit discipline).
**Purpose:** the mandatory Sol deliverable (e) — the exact proposed A4 registration packet: the three `rf.cycle_pattern.imce_*` `declared_budget` row contents as they would be written, byte-ready, clearly marked PROPOSED — NOT REGISTERED; plus the criteria-commit checklist (criteria commit strictly before any outcome access).
**Regeneration note [AP7, A4P binding]:** every row's `reason` string is rewritten to reflect AP1 (target mapping) and AP2 (uniform B≤3 basis across all six cells, superseding the old "≤5 general / ≤3 cancellation-scoped" split). Because `config_hash` is a pure function of `(family, n, reason)`, **every row's hash below is recomputed from the new reason string — none is carried forward from the A4G version.** See EVIDENCE in this wave's return packet for the exact recomputation commands and the before/after hash values.

---

## 1. Row schema (verbatim from `engine/trial_ledger.py`)

`TrialLedger.log_declared_budget(n, family=..., reason=...)` appends exactly this row shape to `data/trial_ledger.jsonl` (the canonical, committed, append-only ledger — `engine/trial_ledger.py` `DEFAULT_PATH = Path("data") / "trial_ledger.jsonl"`):

```json
{
  "ts": "<ISO8601 UTC, set automatically by datetime.now(timezone.utc).isoformat() at the moment of actual registration>",
  "family": "<family name, string>",
  "kind": "declared_budget",
  "n": "<int, the declared multiple-testing budget = frozen cell count>",
  "reason": "<string>",
  "config_hash": "<sha1(f'{family}\\x00{canon({\"__declared_budget__\": n, \"reason\": reason})}').hexdigest()[:16]>"
}
```

`config_hash` is a pure function of `(family, n, reason)` — it does not depend on `ts`, on any outcome, or on any `data/` content. It has been **recomputed exactly** below for each of the three proposed rows (AP7 regeneration — the reason strings changed from the A4G version, so the hashes changed with them), using the repo's own `_canon`/`_hash` functions (`json.dumps(..., sort_keys=True, default=str, separators=(",", ":"))` then `sha1(family + "\x00" + canon).hexdigest()[:16]`), verified against `engine/trial_ledger.py` lines 53–62 and 159–190 — no engine code was executed to write to any ledger; the hash was computed as a standalone pure-function check (see EVIDENCE in this wave's return packet for the exact Python invocation and output). **Only `ts` is genuinely unknowable in advance** (it is stamped at the moment of actual registration) and is left as an explicit placeholder rather than invented.

---

## 2. Proposed row — `rf.cycle_pattern.imce_phase_v0`

```json
{
  "ts": "<set at actual registration>",
  "family": "rf.cycle_pattern.imce_phase_v0",
  "kind": "declared_budget",
  "n": 3,
  "reason": "IMCE preregistration V1.2 (A4P-amended) - 3 cells frozen [A5]: 3 state targets x pooled homebuilder stratum x contrast [M vs family/age prior], all 3 mapped to order_softness [AP1]; predetermined underpowered_accruing [A2]; block basis <=3, cancellation-scoped, uniform across all 6 v0 historical cells [AG5/AG6/AP2]; criteria commit precedes outcome access [G8-B1]. See research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md and research/imce/IMCE_A4G_AMENDMENT_LOG.md.",
  "config_hash": "d4fb6b5f517fe32c"
}
```

**Cell definition (unchanged from contract §1):** 3 state targets × pooled homebuilder stratum × contrast [M vs family/age prior]. **Target mapping SETTLED [AP1]:** all 3 targets track `order_softness` — (a) next cohort state +1rp, (b) next cohort state +3rp, (c) false-repair/relapse within 3rp. Deterministic, outcome-independent construction: `IMCE_A4P_ORDER_SOFTNESS_STATE_CONSTRUCTION_V1.md`. **Settled block basis [AP2, supersedes the A4G MAJ-1 partial settlement]:** all 3 cells in this family are B≤3-entire cancellation-scoped cells under the cell-level ruling in `IMCE_A4G_SIX_CELL_DISPOSITION.md` §1 (Cells 1–3) — the general B≤5 cap describes no cell in this family. **No open item remains for this row** — the AG14/MAJ-5 D5-state mapping that A4G left open is now closed by AP1; this row's `n=3` cells are individually itemizable at actual registration without further design choices.

---

## 3. Proposed row — `rf.cycle_pattern.imce_sync_v0`

```json
{
  "ts": "<set at actual registration>",
  "family": "rf.cycle_pattern.imce_sync_v0",
  "kind": "declared_budget",
  "n": 2,
  "reason": "IMCE preregistration V1.2 (A4P-amended) - 2 cells frozen [A5]: targets {next_local_state_1rp, forward_63d_drawdown_tail} x contrast [M+R vs M], next_local_state_1rp mapped to order_softness [AP1]; predetermined underpowered_accruing [A2]; block basis <=3, cancellation-scoped, uniform across all 6 v0 historical cells [AG5/AG6/AP2]; criteria commit precedes outcome access [G8-B1]. See research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md and research/imce/IMCE_A4G_AMENDMENT_LOG.md.",
  "config_hash": "f76dc44e1f5edc18"
}
```

**Cell definition (unchanged):** targets {`next_local_state_1rp`, `forward_63d_drawdown_tail`} × contrast [M+R vs M]. Only the primary incremental comparison (M+R vs M) supports the synchronization claim (contract §5); secondary comparisons are print-only, zero budget, outside the FDR partition [A7]. **`next_local_state_1rp` (Cell 4) is mapped to `order_softness` [AP1]** — the same next-period cohort state as the phase family's 1-reporting-period target. **Settled block basis [AP2]:** both cells in this family — Cell 4 (`next_local_state_1rp`) and Cell 5 (`forward_63d_drawdown_tail`) — are B≤3-entire, uniformly, per `IMCE_A4G_SIX_CELL_DISPOSITION.md` §1. Cell 5's basis was conditional at A4G (pending its `M_t` feature composition); AP2 settles it: `order_softness` is in the basis for every registered cell, so Cell 5 is B≤3, not the previously-possible B≤5.

---

## 4. Proposed row — `rf.cycle_pattern.imce_risk_v0`

```json
{
  "ts": "<set at actual registration>",
  "family": "rf.cycle_pattern.imce_risk_v0",
  "kind": "declared_budget",
  "n": 1,
  "reason": "IMCE preregistration V1.2 (A4P-amended) - 1 cell frozen [A5]: forward_63d_drawdown_tail x [M vs family/stratum prior]; predetermined underpowered_accruing [A2]; block basis <=3, cancellation-scoped, uniform across all 6 v0 historical cells [AG5/AG6/AP2]; criteria commit precedes outcome access [G8-B1]. See research/imce/IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md and research/imce/IMCE_A4G_AMENDMENT_LOG.md.",
  "config_hash": "3eff3ee65158e41b"
}
```

**Cell definition (unchanged):** `forward_63_trading_day_drawdown_tail` × [M vs family/stratum prior]. Market grading uses QLedger's 63-trading-day ruler and canonical exchange calendar (contract §5). **Settled block basis [AP2]:** B≤3-entire — same reasoning as sync Cell 5 (§3 above). See `IMCE_A4G_SIX_CELL_DISPOSITION.md` §1 (Cell 6).

---

## 5. BH-FDR partition registration (accompanies, does not replace, the three rows above)

Per contract §1/§11 (A6): all 6 cells across the three families above run under **one** BH-FDR partition, `imce_hist_v0`, at **q = 0.10**. This is not itself a `declared_budget` row (it is a separate FDR-family registration act, also part of A4/IMCE-03, mechanically distinct from the per-family trial-budget rows above) but is named here for completeness, since a `declared_budget` row without its FDR partition registered alongside it would be an incomplete A4 act:

```json
{
  "fdr_partition": "imce_hist_v0",
  "q": 0.10,
  "member_cells": 6,
  "note": "single BH-FDR partition over the union of all 6 historical cells; the three rf.* family names are trial-ledger provenance labels, not separate FDR partitions",
  "runner_obligation": "the runner asserts exactly the six registered cell IDs before BH correction, and applies exactly one correction at q=0.10 over their union - no seventh cell may silently enter the denominator [AP5]",
  "producer": "A4 registration session - no current repo writer for this shape; deliberate, this is proposed content only [M6 fix]"
}
```

**[M6 fix]** Unlike the `declared_budget` row (§1, written by `engine/trial_ledger.py`'s `log_declared_budget`), this repository has **no existing writer** for an FDR-partition registration record — no module, script, or schema in `engine/` or `scripts/` currently accepts or persists this shape. That absence is deliberate to this proposal, not an oversight: the FDR-partition registration mechanism itself (where it lives, what module writes it, what file it lives in) is unspecified A4/IMCE-03 design work, separate from the already-built `declared_budget` mechanism. **[AP5]** No new writer is built by A4P either — out of scope for a records-only wave. A future A4 registration session must either build that writer or fold this content into an existing mechanism before this JSON shape has anywhere real to go; whichever mechanism is chosen, it must enforce the runner obligation above as a hard assertion, not merely document it.

---

## 6. Criteria-commit checklist — criteria commit STRICTLY before any outcome access

Per contract §15a ("Two-commit discipline: the criteria commit strictly precedes the runner/outcome commit") and freeze G8-B1. **Every item below must be true and committed BEFORE the first outcome value of any kind is accessed for any of the 6 historical cells or any prospective cell.** This checklist is itself part of the criteria commit — the actual A4 registration commit must include all of the following, in this order, in one commit or a tightly sequenced series that entirely precedes any runner/outcome commit:

- [ ] **Contract V1.2 frozen** — `IMCE_PREREGISTRATION_AND_EVALUATION_CONTRACT_V1.md` and its YAML projection, as amended by A4G and A4P (this wave), committed to `origin/main`.
- [ ] **All 18 A4G rulings AND all 7 A4P rulings encoded** — `IMCE_A4G_AMENDMENT_LOG.md` present, every AG1–AG18 and AP1–AP7 entry cross-referenced against the amended contract sections.
- [ ] **Six-cell disposition recorded** — `IMCE_A4G_SIX_CELL_DISPOSITION.md`, all six cells settled at B≤3 uniformly (AP2), the phase-family target-to-D5-state mapping closed (AP1) — **no open item remains in this document.**
- [ ] **`order_softness` deterministic construction frozen** — `IMCE_A4P_ORDER_SOFTNESS_STATE_CONSTRUCTION_V1.md` present and referenced normatively by the contract (AP1). No outcome-selected threshold, no grid search, no issuer-specific tuning anywhere in the construction.
- [ ] **Source/boundary table recorded** — `IMCE_A4G_SOURCE_BOUNDARY_TABLE.md`, including every macro block boundary's receipt status (`not_yet_receipted` where applicable — no invented dates) and the Treasury CSV/XML archive receipt (§7, AP6 — Sol-attested + session-`S`-grade, storage basis unsettled, no persistent ingestion).
- [ ] **Three `declared_budget` rows appended** to `data/trial_ledger.jsonl` — the exact contents in §2–§4 above, with `ts` populated at the moment of the actual write, and `config_hash` matching the recomputed values above (not the superseded A4G-era hashes).
- [ ] **FDR partition registered** — `imce_hist_v0` at q=0.10, per §5 above, including the AP5 runner obligation as an enforced assertion (not documentation only).
- [ ] **`n_effective_blocks` computation path frozen** — B ≤ 3 (cancellation-scoped), **uniform across all 6 registered cells [AP2]** — the general B≤5 cap describes no cell registered by this contract; no ρ-based estimator used as N; the precision-diagnostic field name (AG4) fixed if it is to be computed at all.
- [ ] **All 3 `imce_phase_v0` targets and `imce_sync_v0` Cell 4 mapped to `order_softness`, with a registered deterministic construction** (contract §15/§15a, AP1) — **BINDING and already SATISFIED** by this wave; a future target added to this contract must independently satisfy the same stop condition.
- [ ] **Bootstrap draws and seed registered: 800 draws, seed 7 (CPI house default) [AP4]** — already settled by this wave, not a discretionary registration-time choice.
- [ ] **Preregistered minimum prospective share registered: 100% [AP3]** — already settled by this wave, not a discretionary registration-time choice.
- [ ] **Come-back date published on the B=3 uniform cancellation-scoped basis: ~2149 [AP2]** — supersedes the A4G ~2153 (B=5) figure, which itself superseded the pre-A4G ~2145 figure. There is no basis election to make; AP2 settled B=3 as the basis for all six registered cells.
- [ ] **`config_hash` recorded** (contract §15a) — computed at registration from the actual committed contract content, not the exact-but-narrower `declared_budget` row hashes in §2–§4 above (those are genuinely exact, computed against `engine/trial_ledger.py:53-62` — they are simply a different, narrower hash over `(family, n, reason)` only, not the contract-level `config_hash`).
- [ ] **Repository pin recorded** (contract §15a) — the exact commit SHA of the contract as registered.
- [ ] **Zero outcome access verified** — a final, explicit statement in the actual A4 registration commit message or record that no forward return, drawdown, Brier score, calibration statistic, p-value, correlation, or regression was computed on any cell before this checklist's items above were all committed. **No outcome access occurred in the drafting of this packet or any of its A4G/A4P predecessors** — the `order_softness` construction (AP1) is a rule, never a computed value, per its own §0/§5.

**Only after every box above is checked, committed, and pushed may any runner/outcome commit occur.** This document does not check any box — it is the proposed content and checklist for a future session to execute and verify. **After AP1–AP7, this checklist's remaining items are entirely mechanical** (repository pin, `ts` stamp, contract-level `config_hash`, the actual `data/` write, and building the FDR-partition writer) — no further design or scoping choice remains open for the three declared trial families.

---

## 7. What this document does NOT do

- Does not write `data/trial_ledger.jsonl`.
- Does not access any outcome, return, drawdown, Brier score, or price series.
- Does not register the `imce_hist_v0` FDR partition.
- Does not build the FDR-partition-registration writer (AP5 — remains an A4/IMCE-03 build item).
- Does not compute the contract-level `config_hash` (only the three row-level `config_hash` values, which are a narrower, different hash over `(family, n, reason)` only).
- Does not itself constitute A4 registration — it is the proposed, byte-ready content for a future, separately-authorized registration act to execute verbatim or amend before executing.

---

**This document authorizes nothing beyond itself.** The next authorized act on this family is actual A4 registration (IMCE-03), executing the checklist in §6 above, as its own separately-authorized wave.
