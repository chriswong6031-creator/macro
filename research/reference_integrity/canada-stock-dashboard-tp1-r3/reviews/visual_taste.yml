# Critic receipt (RIG V1 §6) — visual / art direction, R3 REVIEW-ORDER RE-RUN.
#
# Replaces the earlier r3 visual_taste receipt, whose review ORDER could not be
# proven: its pass-1 findings were never committed before the rationale reveal.
# The superseded receipt is preserved verbatim at reviews/superseded_review_order/
# and every one of its findings is carried in continuity.yml — nothing evaporates.
#
# Run under Sol CONTINUE (carrier theme-parity-tp1-canada-20260828-sol-001, ts
# 1788118073.730499) with a genuinely fresh critic that had never seen the
# surface, the prior verdicts, the RIG records, or any repair narrative. THIS
# FILE IS THE FIRST-PASS FREEZE: committed BEFORE any rationale or constraint is
# revealed. Git ancestry, not the coordinator's word, proves the ordering.
schema: mastermind.rig_review.v1
reference_id: canada-stock-dashboard-tp1-r3
role: visual_taste
artifact_sha: e509db03ee5f7bb5b2ef1140935d57706c858370

reviewer:
  identity: "rig-r3rr-critic-b-verdigris-lark-2264"
  model: opus
  independent_of_author: true

quarantine:
  first_pass_inputs:
    - user/business job (redacted packet)
    - capability ledger (redacted packet)
    - production-before artifact (comment-redacted composer at c819f6428f91)
    - proposed-after artifact (comment-redacted source at the frozen SHA + 24 rendered cells)
  rationale_received_after_first_pass: true
  packet_construction: >
    Same redacted packet as the product-regression role — all code comments
    stripped, the baseline's chronology note and design_lineage constraints
    removed, every TP-1 string scrubbed, automated leakage scan clean before any
    critic was spawned.
  critic_declaration: >
    Honored in full, on both sweeps. The critic states it read nothing under
    research/reference_integrity/ (any cycle), no THEME_PARITY docs, no plans, no
    design doctrine, no acceptance record, no PR or Slack content and no other
    reviewer's output; ran no git history command; did not open the repository
    templates/ or site/ trees; and cited every source claim to the packet copies.

first_pass:
  frozen_at: "2026-08-30T22:20:00Z"
  verdict: PASS_WITH_CONDITIONS
  coverage: >
    Both sweeps. The coordinator refused a first pass that left ZH and 390
    unadjudicated on a theme-and-locale parity change and sent the critic back
    for them; ZH desktop, both 390 locales, the isolated breadth cells and the
    remaining state cells were then opened. Three gaps remain that cannot be
    closed because the evidence does not exist in the matrix — those are the
    subject of VTB-409.
  findings:
    - id: VTB-401
      severity: major
      finding: >
        The leadership breadth bar encodes basket size but reads as leadership
        strength, and contradicts the rank beside it. Unlabelled, neutral grey,
        no legend, axis, tick or "out of what". Measured fills identical in both
        themes — 79.1 / 27.7 / 98.5 / 59.7 / 98.5 percent, i.e. names over
        max-names-in-view. Inside a table headed THEME RANK, directly beneath the
        Theme #1..#5 ordinal, the LONGEST bars belong to rank #3 and rank #5 and
        the shortest to rank #2. A subscriber doing the stated job scans the
        strongest pre-attentive mark in the section and reads the wrong themes as
        leaders; the disambiguating digit is 1,082px away at 11.7px. The isolated
        breadth cells make it worse, not better — cropped to the ranking, the
        five grey bars are the only graphic and the contradiction is the whole
        picture. At 390 it degrades further: the bar underlines an
        ellipsis-truncated ticker list, so it reads unambiguously as a text rule.
    - id: VTB-402
      severity: major
      finding: >
        The denominator is the visible-five maximum, is never stated, and the
        extremes are unevidenced. max is computed over the first five themes, so
        the bar answers "share of the largest of the five currently shown" — a
        denominator that appears nowhere on screen and moves when the top five
        move. The Expand-leadership modal renders the full list from the same
        data with no bar at all, so the same theme has a length in one surface
        and nothing in the other. At the top of the scale the fill cannot reach
        its own track (200px max-width in a 206px track; 203/206 measured in both
        themes, 113/116 at 390) — the true maximum never looks maximal. The
        isolated breadth cells do NOT close the extremes: all 24 cells carry the
        same 12/4/15/9/15 dataset, so no zero-count and no null-count theme
        appears anywhere, and that behaviour is verified from source only.
    - id: VTB-403
      severity: major
      finding: >
        The IN FAVOUR blue is the one accent that never adapts — not to theme,
        not to locale. Across all four theme x locale desktop cells, lanes 1 and
        4 flip for CJK convention and lanes 1, 3, 4 are all re-inked for the
        white canvas, while lane 2 is rgb(41,90,234) in BOTH light locales and
        rgb(122,167,224) in both dark ones. At saturation ~0.82 against a white
        panel it is the highest-chroma mark in the light Act-Now band, where its
        three siblings sit at ~0.475-0.483. The stance ladder is Buy Now → In
        Favour → Bottoming Watch → Reduce, so in light the SECOND rung is the
        loudest and first-second triage is pulled to the weaker stance. Root
        cause visible in source: the near lane resolves through --ink-link while
        its three siblings resolve through --ink-pv-*.
    - id: VTB-404
      severity: minor
      finding: >
        Light lane columns have no bottom edge, so ragged lane lengths read as an
        unfinished panel. In light the four lanes are open columns divided by
        three 1px verticals and nothing else; the shorter lanes leave 60-100px of
        unbounded white below their last row. In dark the same lanes are enclosed
        cards, so the empty tail reads as padding. Light makes the shorter lanes
        look truncated — the opposite of what the lane counts are asserting.
    - id: VTB-405
      severity: minor
      finding: >
        The light Act-Now header wash is below perceptual threshold — rgb(247,249,248)
        on rgb(255,255,255), an 8/255 step — so light carries stance on a 2px rule
        alone, where dark carries it twice (accent rule plus a distinct header
        band). For a red/green-deficient reader the light lanes are separated by
        a 2px rule and an 11px uppercase label only. This matters more in ZH,
        where the rule's meaning has inverted.
    - id: VTB-406
      severity: minor
      status: WITHDRAWN_IN_FIRST_PASS
      finding: >
        Withdrawn with resolving evidence, not silently dropped. The 119px EN→ZH
        mobile height delta is benign CJK compaction, not a dropped element: band
        analysis shows identical structure and gap signatures, with the delta
        accumulating in three places rather than at one missing block, and the
        51px step is the Prophet control row wrapping to two rows in EN and
        fitting on one in ZH. Both locales show both segmented controls, all four
        options, correct selected state.
    - id: VTB-407
      severity: major
      finding: >
        The leadership rank label is hardcoded English and appears untranslated
        on the ZH board. Every ZH leadership row reads "Theme #1 / 白银与特许权金"
        etc., while the column header, basis chip, stance chips, section title
        and theme names are all translated. Source confirms it is not a rendering
        accident — the string is built outside the bilingual helper, and the same
        hardcoded string is reused in the modal, so it reproduces in the expanded
        view. Present in both dark ZH and light ZH. The themes-only-rank
        capability requires the rank basis to be VISIBLE; on the ZH board its
        label is the one word a ZH reader cannot read, on the highest-value row
        element. A small, unambiguous repair with no design judgement in it.
    - id: VTB-408
      severity: major
      finding: >
        Under ZH the same hue means opposite things in the same visual field. The
        stance palette flips for CJK convention (red = buy, green = avoid), but
        the direction ink does not: the LIVE dot measures byte-identical green
        between EN and ZH, and the stylesheet pins up to --ok and down to --act
        with !important, which the ledger names as precisely the mechanism for
        keeping Western green-up under ZH. Both halves are individually
        defensible; together they put, on one ZH screen, green = "reduce / avoid
        this group" AND green = "price up / feed live", red = "buy this group"
        AND red = "price down", with nothing signalling which system a given
        green belongs to. ATTRIBUTION NOTE FROM THE CRITIC: with no ZH baseline
        in the matrix it cannot tell whether this proposal introduced the
        collision or inherited it, and it deliberately did not go looking. Judged
        on the artifact alone it is a real defect needing an owner's ruling.
    - id: VTB-409
      severity: minor
      classification: evidence-matrix defect
      finding: >
        The evidence matrix is asymmetric on exactly the axes under review. Three
        concrete holes. (a) The filter-miss cell has no dark counterpart, so a
        named core capability is evidenced in one theme only. (b) ZH has NO state
        cells at all — no ZH filtered, filter-miss, all-candidates, table-view,
        modal or breadth cell — so every interactive state was judged for EN and
        then assumed for ZH. (c) There is no ZH baseline, which is what makes
        VTB-408 unattributable. This is the reason two findings carry attribution
        caveats rather than verdicts.
    - id: VTB-410
      severity: minor
      classification: evidence-matrix defect
      finding: >
        Several cells are partly occluded by transient site chrome, including
        over the region under review. The theme-toggle sun/moon animation is
        mid-flight in both baselines and in four ZH/mobile cells, sitting over
        the Act-Now band; in the EN mobile cells it covers the BUY NOW lane's
        first row so the group name is unreadable — the one lane row visible at
        390. Separately a floating widget overlaps a card's rank badge in the
        all-candidates cell and the last table row in the table-view cell.
        Neither is a product defect, but a cell that hides the surface it is
        filed to prove is weak evidence.
    - id: VTB-411
      severity: minor
      finding: >
        Grid and Table use different stance vocabularies for the same tickers.
        The population is preserved correctly, but one ticker is BUY in the card
        grid and ENTRY OPEN in the table, and four others are NEAR in the grid
        and SETTING UP in the table. A user who learns the grid's stance ladder
        cannot find those words after toggling to Table — the one interaction the
        toggle exists to support.
    - id: VTB-412
      severity: minor
      finding: >
        The table's Off High column spends the page's avoid-red on a quantity
        that is negative by construction. All five values render in the exact
        colour of the light REDUCE/AVOID lane accent, so the colour carries zero
        information in that column while diluting red's meaning elsewhere on the
        same screen. The neighbouring momentum column, where sign IS informative,
        gets the same treatment — correct there, which makes the Off-High usage
        look deliberate rather than incidental.
    - id: VTB-413
      severity: minor
      finding: >
        Card sector labels stay English on the ZH board while the same sector is
        translated one section above — the Act-Now lanes render Chinese sector
        names while the Prophet cards for those same sectors render English. The
        population-filter capability asks the user to filter by sector and then
        read the filtered cards; in ZH the filter chip and the card label are in
        different languages, so the match cannot be made by name. Attribution:
        the card body is legacy markup the composer restyles rather than authors,
        so this is very likely inherited — recorded as minor for that reason.
  dual_theme_adjudication:
    claim_tested: "light is a genuine second art direction, not a token substitution"
    result: SURVIVES_FALSIFICATION
    mechanisms_differing_in_kind:
      - "lane containment: dark keeps enclosed cards; light deletes the enclosure for open hairline-divided columns (verified in pixels — three full-height 1px verticals, no lane outline)"
      - "accent delivery: dark border-top flush with the card edge; light a detached ::before rule with 10px of white above it"
      - "semantic ink: light re-authors stance colours through --ink-pv-* rather than inverting dark hues; measured hue/chroma sets are different families in both locales"
      - "elevation: light re-specifies panel and card surfaces separately and replaces the dark light-emitting ring with its own card rule — glow to shadow, made explicitly"
      - "responsive: light carries its OWN breakpoint corrections for the new column model, behaviour that exists only because light chose a different material model — the strongest single evidence that light was designed rather than derived"
    dark_independently: >
      Sound. Cards on a deeper gradient field, stance colour held at mid-luminance
      so no accent burns, five distinct type steps each doing one job, vertical
      rhythm tightened against baseline with no row below a 32px hit target.
    mobile_390: >
      Re-composes rather than shrinking — four-lane row becomes a one-lane view
      with a working segmented switcher, single-column cards, and a meter
      re-scaled by a deliberate responsive unit rather than a percentage hack.
      Both locales hold.
    conclusion: >
      Light's failures are not "dark inverted" — they are one token left behind
      (VTB-403), one edge condition (VTB-404) and one wash below threshold
      (VTB-405).
  strengths:
    - The whole presentation moved out of a runtime-injected string into a real
      stylesheet; the JS that remains sets state classes and one genuinely
      data-dependent inline value. The correct division, and the most valuable
      thing in the packet.
    - Missing is not rendered as zero — null count yields no bar, no class and an
      em-dash, on exactly the axis the ledger flags.
    - >-
      The meter's mobile behaviour is properly authored — a responsive unit
      reduction yields a 116px track with proportions preserved. Whatever is
      wrong with what the bar MEANS, its responsive engineering is right.
    - Sparkline colour is stance-coded, not decorative, and consistent — the
      critic went looking for a dishonest chart colour and did not find one.
    - >-
      Filter states are honest — the population is never silently narrowed, and
      both the filtered count and the filter-miss escape are bilingual in source.
    - The light lane band is the best-authored region in the packet.
    - Type sits on a real scale with tabular numerals wherever numbers are
      compared down a column.
  critic_verdict_reasoning: >
    Held at PASS_WITH_CONDITIONS rather than escalating to BLOCK, with the
    reasoning made visible: VTB-407 is a one-line repair with no design judgement
    in it, and VTB-408 — the only finding with blocker-shaped consequences —
    rests on an inference chain rather than a photographed up-tick, and may be
    inherited site convention. Escalating on an unphotographed contradiction
    would violate the commission's own "no claim may rest on assumption" rule.
  gaps:
    - "green-up/red-down under ZH is never directly shown — every price chip in all 24 cells renders flat, so the up/down rules are not exercised in any cell in either locale"
    - "breadth zero and missing extremes remain source-only; no zero-count or null-count theme appears in any cell"
    - "ZH interactive states do not exist in the matrix; those states were judged in EN and deliberately NOT extrapolated to ZH — unjudged, not passed"
    - "VTB-408 unattributable without a ZH baseline"
    - "no WCAG ratios — the token file is not in the packet, so colour findings are about hue/chroma family and material step, not compliance thresholds"
    - "motion, hover, focus-visible and keyboard traversal are specified but uncaptured; modal focus and Escape handling were read, not exercised"
    - "entitled/authenticated behaviour is out of the matrix by the packet's own declaration"

second_pass:
  # Run AFTER freeze commit a26efe0336dd and after the rationale/constraints reveal.
  verdict: PASS_WITH_CONDITIONS
  continuation_note: >
    DISCLOSED CONTINUATION. The host purged the pass-1 critic context after the
    freeze. The critic read the frozen first_pass block AS AN ARTIFACT — as the
    review standard required it to attack — and re-derived every amendment from
    the packet, the CSS bytes and the pixels. It records that this cut both ways:
    it could not consult pass-1 working (so VTB-406 rests on its recorded evidence
    rather than fresh measurement), but it also re-measured VTB-401 and VTB-402
    from scratch instead of trusting figures it would otherwise have remembered
    writing — which is HOW BOTH OF ITS OWN ERRORS SURFACED.
  severity_token_note: >
    The critic used "nit" for four downgrades (VTB-410, VTB-411, VTB-412,
    VTB-413). That is not a legal RIG severity; per the critic's own instruction
    ("read all four as minor, out of boundary") they are recorded at the schema
    floor "minor" with the critic's word preserved.
  self_corrections_not_prompted_by_the_reveal: >
    Two amendments correct the critic's OWN pass-1 evidence, found by
    re-measurement and prompted neither by the coordinator nor by the reveal. This
    is flagged so the counts are not misread as a critic softening under
    disclosure.
  amendments:
    - finding: VTB-401
      action: upheld
      severity: major
      basis: re-verified-evidence
      note: >
        The reveal is correct that the bar is the authorized repair and that the
        repair made the encoding truthful; that is orthogonal to this finding.
        CORRECTION OF RECORD against the critic's own pass-1 numbers: measured
        against the correct 200px inner channel the fills are exactly
        80.0/27.0/100.0/60.0/100.0 percent, not the 79.1/27.7/98.5/59.7/98.5
        filed in pass 1, which used the 206px outer track as denominator. The
        correction SHARPENS the finding — the longest bars still belong to rank #3
        and rank #5 and the shortest to rank #2, and they are now exactly
        full-length, so the contradiction with the ordinal is at maximum strength
        on the two rows where the ordinal is weakest. Second correction: the
        pass-1 phrase "the strongest pre-attentive mark" overclaims; measured, the
        bold theme names and outlined stance chips are stronger. The bar is the
        only MAGNITUDE-encoded mark, and length is the channel that reads as
        ranking. The defect survives both corrections. At 390 it degrades further,
        underlining an ellipsis-truncated ticker list and reading as a text rule.
    - finding: VTB-402
      action: downgraded
      severity: minor
      basis: re-verified-evidence
      self_refuted: true
      note: >
        ENTIRELY THE CRITIC'S OWN ERROR, refuted on bytes and pixels. The pass-1
        allegation that "the fill cannot reach its own track, the true maximum
        never looks maximal" is FALSE. Track left:14px width:206px; fill
        left:17px max-width:200px — a symmetric 3px inset, the standard inset
        progress-bar idiom. At breadth 100 the fill measures exactly 200px, 100.0
        percent of the inner channel, in both themes. The pass-1 figure of 203/206
        came from using the outer track as denominator. What SURVIVES on
        re-verified composer bytes: the denominator is the max over the visible
        five, appears nowhere on screen, and MOVES when the top five move, so a
        theme's bar can change length with no change to its own data; and the
        modal renders the same themes from the same data with no bar at all.
        Real, but secondary to an already achromatic low-authority mark whose
        disambiguating count sits in the NAMES column.
    - finding: VTB-403
      action: upheld
      severity: major
      basis: re-verified-evidence
      note: >
        Direct answer to the coordinator's question: the ratified rebinding does
        NOT change severity for light. The reveal establishes authority for the
        BINDING — near moved off the stance family because the tokens derive near
        from buy and collapsed BUY with IN-FAVOUR — and the critic accepts the
        collapse was real and breaking it correct. But the finding is about what
        the binding LEFT BEHIND on the white canvas, and ratifying a mechanism
        does not ratify the resulting light value. Measured: in dark, near is
        chromatically ordinary (spread 102, mid-pack behind BOTTOMING at 176). In
        light, the re-authoring pulled all three sibling inks into a muted 71-118
        spread family and left near at 193 with saturation 0.825 against siblings
        at 0.644-0.652 — making the SECOND rung of the stance ladder the loudest
        mark in the band. A theme-specific regression existing only in light,
        exactly the class the dark/light art-direction law exists to catch. NOT a
        legibility problem — contrast 5.30, tied with siblings — but a chroma-rank
        problem. CORRECTION OF RECORD: the pass-1 headline "never adapts, not to
        theme, not to locale" is wrong on the theme half and self-refutes in its
        own body; near does differ between dark and light. What it never does is
        adapt to LOCALE, and its light value was never re-authored into the light
        stance family.
    - finding: VTB-404
      action: upheld
      severity: minor
      basis: re-verified-evidence
      note: >
        Re-verified on light EN desktop — four open columns divided by hairline
        verticals, no lane outline, no bottom rule; shorter lanes leave unbounded
        white below their last row. The reveal has no bearing: this is light's own
        material model, neither inherited nor out of boundary.
    - finding: VTB-405
      action: upheld
      severity: minor
      basis: re-verified-evidence
      note: >
        Re-verified. Light lane-header band rgb(247,249,248) on a white panel, an
        8/255 step; ZH light equivalent rgb(250,247,248). Light carries lane stance
        on the 2px accent rule plus the coloured label; dark carries it twice.
    - finding: VTB-406
      action: withdrawn
      severity: minor
      basis: pass-1-evidence-not-re-run
      note: >
        Withdrawal confirmed. The critic re-read its recorded resolving evidence,
        found nothing in the reveal touching it, and states plainly that it did
        NOT re-measure the 119px delta in this pass.
    - finding: VTB-407
      action: downgraded
      severity: minor
      basis: mixed
      note: >
        Defect CONFIRMED IN BYTES on both surfaces — the rank string is built
        outside the bilingual helper and the identical literal is repeated in the
        modal row builder, while name, stance, column header and basis pill on the
        same row all route through it. Not a rendering accident, and it reproduces
        in the expanded view. Severity moves for a STATED reason: the critic
        accepts ON AUTHORITY CONTEXT ALONE that the string is byte-identical to
        deployed production and the composer was frozen, and did not diff against
        deployed bytes. Given that, it is inherited estate debt this change was
        forbidden to touch, and holding a presentation extraction to a repair it
        was not permitted to make is not defensible. DOWNGRADED, NOT WITHDRAWN —
        inherited and out of boundary changes who owes the fix and when, but does
        not make an untranslated word on the highest-value row of a bilingual
        board acceptable, and the capability explicitly requires the rank basis to
        be VISIBLE.
    - finding: VTB-408
      action: downgraded
      severity: minor
      basis: mixed
      note: >
        Direct answer to the coordinator's question: THIS IS A CRITICISM OF THE
        RATIFIED LAW, NOT A DEFECT IN THIS CHANGE. The collision is real and
        tighter than pass 1 could show — on one ZH light screen the LIVE dot
        measures rgb(47,138,82), byte-identical to EN, while REDUCE/AVOID stance
        ink measures rgb(37,117,83): two greens within ~10-21 per channel, one
        meaning "live / price up", one meaning "reduce, avoid". Same in dark. The
        reveal supplies what the pass-1 attribution note said was missing, closing
        the gap and REMOVING THE BLOCKER SHAPE. What does not close: nothing on
        the surface signals which colour system a given green belongs to. The law
        is defensible in the abstract — quote inks must not flip, stance must flip
        for a CJK reader — but is implemented with no disambiguating cue, in
        near-identical greens rather than visibly different families. Carried as a
        minor observation for the authority's docket, not a condition on this
        change.
    - finding: VTB-409
      action: upheld
      severity: minor
      basis: re-verified-evidence
      note: >
        Direct answer: CARRY AS NAMED DEBT, DO NOT BLOCK — but one clause is a
        live constraint on what this cycle may claim. Three holes re-confirmed in
        the manifest: filter-miss is light-only with no dark twin; every ZH entry
        is a default-state cell, so there is NO ZH filtered, filter-miss,
        all-candidates, table-view, modal or breadth cell; and the baseline
        declares the ZH locale axis a gap outright. It should not block because
        the missing cells evidence product states this change did not touch —
        composer frozen, filter/table/modal logic byte-identical — so the gap
        BOUNDS what may be claimed rather than concealing a likely regression. The
        bounding must be written down verbatim: every ZH interactive state was
        judged in EN and not extrapolated; those states are UNJUDGED, NOT PASSED,
        and that clause must travel with any acceptance record.
    - finding: VTB-410
      action: downgraded
      severity: minor
      critic_severity_word: nit
      basis: re-verified-evidence
      note: >
        Downgraded on a provenance note unavailable in pass 1: the proposal record
        documents and bounds the artifact — named full-page shots use a >=1500ms
        settle and are orb-free and authoritative, and only the harness cells
        (400ms hardcoded) can carry it. A harness artifact with a clean
        authoritative alternative for every EN desktop claim, which is why the
        critic measured the light EN band from the full-page shot without touching
        an occluded cell. It bites only where the harness cell is the SOLE
        evidence, i.e. the ZH axis — already carried under VTB-409.
    - finding: VTB-411
      action: downgraded
      severity: minor
      critic_severity_word: nit
      basis: re-verified-evidence
      note: >
        Boundary verified by the critic's OWN grep, not asserted: the table
        vocabulary returns zero matches across both changed files, so it is
        legacy/engine-emitted copy the composer restyles rather than authors. The
        user-facing problem stands but is unreachable from this change.
    - finding: VTB-412
      action: downgraded
      severity: minor
      critic_severity_word: nit
      basis: authority-context-alone
      note: >
        Boundary accepted on authority context alone, corroborated by the same
        zero-match grep; the critic did not verify which upstream file emits it.
    - finding: VTB-413
      action: downgraded
      severity: minor
      critic_severity_word: nit
      basis: authority-context-alone
      note: >
        Accepted on authority context alone, consistent with the critic's pass-1
        attribution note which reached the same conclusion independently; not
        re-verified in this pass. The critic notes the consequence is worse than
        the severity suggests: the population-filter capability asks a ZH user to
        match a Chinese filter chip against an English card label.
  dual_theme_adjudication_restated:
    claim_tested: "light is a genuine second art direction, not a token substitution"
    result: SURVIVES_FALSIFICATION
    strongest_evidence: >
      Light carries its own breakpoint corrections for a column model that exists
      only because light chose a different material model — a token-substitution
      light cannot need its own responsive corrections.
    revision_for_the_record: >
      Pass 1 called VTB-403 "one token left behind". Re-measurement shows worse: it
      INVERTS THE INTENDED STANCE RANKING IN LIGHT ONLY, because the light
      re-inking lowered the other three lanes' chroma while leaving this one at
      full value. Category unchanged; consequence is a rank error at glance tier.
    light_failures_characterised: >
      Light's failures are the residue of an INCOMPLETE LIGHT RE-AUTHORING, not
      "dark inverted" — a token the light stance family left behind, an edge
      condition the open-column model introduced and did not finish, and a wash
      specified below its own threshold. All three light-only, all three
      consequences of light having genuinely chosen a different material system.
    en_zh_parity: >
      Re-confirmed on pixels in both themes — stance flip present and correct in
      light ZH and dark ZH, structure and density hold. The only locale-axis
      defects are VTB-407 and VTB-413, both inherited.
  open_carried_as_named_debt:
    - "VTB-401 major, OPEN, IN BOUNDARY — the one finding attached as an actual condition. Unlabelled magnitude encoding contradicting the adjacent ordinal; degrades further at 390. Fixable in the governed stylesheet."
    - "VTB-403 major, OPEN, IN BOUNDARY — light link-token lane ink out-ranking BUY NOW by chroma. Fixable in tokens; the authority should rule, since the rebinding that caused it was theirs."
    - "VTB-402 minor, OPEN, in boundary — undeclared moving denominator; inline-vs-modal encoding disagreement."
    - "VTB-404 and VTB-405 minor, OPEN, in boundary — light-only material defects."
    - "VTB-407 minor, OPEN, out of boundary — inherited untranslated rank label; needs an estate owner and a date, not silent closure."
    - "VTB-408 minor, OPEN as criticism of ratified law — authority docket, not a condition on this cycle."
    - "VTB-409 minor, OPEN as evidence debt, with its live clause: ZH interactive states are UNJUDGED, NOT PASSED, and that must travel with any acceptance record."
    - "VTB-411, VTB-412, VTB-413 minor (critic word: nit), OPEN, out of boundary — engine/legacy copy debt."
    - "VTB-410 minor (critic word: nit) — harness settle artifact; residual folded into VTB-409."
    - "VTB-406 — closed (withdrawn), not carried."
  observations_not_findings:
    - "A contrast-ratio gate would PASS VTB-403: light near sits at 5.30, tied with siblings at 5.29-5.32. The defect lives entirely in the chroma channel, so any accessibility check on this surface reports clean. Catching this class needs a chroma-rank or saturation-budget check per semantic band, not a contrast check."
    - "The near-to-link rebinding has a second consequence the critic saw but did not scope: the leadership HOLD stance chip renders in the same link-blue, so on the leadership panel a wayfinding token also carries a stance. Whether the ratified rebinding intended that is for the authority."
    - "The breadth denominator is UNSTABLE, not merely undeclared — a theme's bar can change length with no change to its own membership. If the bar is kept, that argues for a fixed denominator over a legend."
    - "No zero-count and no null-count theme appears in any of the 24 cells; all carry the same dataset. The missing-is-not-zero law the repair exists to serve is verified in SOURCE ONLY, never photographed. The critic names this the gap it would most want closed before the next cycle."
    - "PROCESS: two of thirteen pass-1 findings contained a measurement error and a self-contradicting headline that survived a full first pass; both were caught only because pass 2 re-measured rather than re-read. A two-pass structure permitting pass 2 to reason from pass 1's numbers would have propagated both."
  bounding_note: >
    The critic did not exercise the surface. All measurements come from committed
    PNGs and committed source bytes. Motion, hover, focus-visible, keyboard
    traversal, modal focus and Escape remain uncaptured; ZH interactive states are
    absent from the matrix entirely. The verdict is bounded by that and must not
    be read as covering them.
