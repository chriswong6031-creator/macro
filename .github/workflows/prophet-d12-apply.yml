name: prophet-d12-apply

on:
  push:
    branches:
      - sol/prophet-us-d12-pack-tip-20260827

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          filter: blob:none
          sparse-checkout-cone-mode: false
          sparse-checkout: |
            /.github/workflows/prophet-d12-apply.yml
            /scripts/d12_apply_patch.py
            /scripts/build_prophet_live_pack.py
            /tests/test_prophet_live_pack_d12.py
            /engine/prophet_live/**
            /lib/nyse_calendar.py
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Apply exact D12 patch
        run: python scripts/d12_apply_patch.py
      - name: Run focused D12 GREEN
        run: |
          pip install --quiet pytest pandas
          python -m pytest tests/test_prophet_live_pack_d12.py -q
      - name: Commit implementation and remove one-shot machinery
        run: |
          git config user.name "Sol D12 mechanical apply"
          git config user.email "audit@invalid.local"
          git rm .github/workflows/prophet-d12-apply.yml scripts/d12_apply_patch.py
          git add scripts/build_prophet_live_pack.py
          git diff --cached --check
          git commit -m "fix(prophet-live): quarantine impossible pack tips before gating"
          git push origin HEAD:sol/prophet-us-d12-pack-tip-20260827
