---
workstream: WS:PROPHET-CONDITIONAL-FUSION
session: prophet-us-conditional-fusion-pr0 (worktree prophet-us-conditional-fusion-a4e2ae);
  addendum by claude/si-pit-context-dim (same-day si-pit chip session — amended in place
  because latest-wins handoff ranking drops a suffixed same-day filename)
model: fable
ended_because: ci_handoff
prs:
  - "#5593"
  - "#5602"
decisions:
  - DEC:PROPHET-ZERO-AUTHORITY-SUPERSEDED-BY-EARNED-CONDITIONAL-AUTHORITY
mission: >
  PR-0 of the CEO-commissioned Prophet US Conditional Intelligence Fusion program:
  record the ruling superseding blanket zero-authority, census the estate (authority,
  PIT coverage, lineage, pathology), freeze the champion/challenger arena + outcomes +
  validation protocol, define the four-layer architecture and interop with #5578/#5583,
  run the required independent adversarial review, and ship docs-only.
state_before: >
  No fusion program existed. engine/us_board_rank.py held the blanket
  ZERO_SCORE_AUTHORITY doctrine (14 families); the roadmap's §3 one-axis ladder was the
  only promotion path; #5578 (Live Entry Radar) and #5583 (Stock Identity) both open and
  armed, each reserving the fusion role behind gates; context-vector store at 4 stamped
  days; live priority score never graded.
changed:
  - path: research/PROPHET_CONDITIONAL_FUSION_MASTERPLAN_BY_FABLE.md
    what: NEW — the full PR-0 deliverable set (13 items), incl. §17 adversarial-review
      disposition table (11 blockers resolved in-doc before the PR opened)
  - path: agentos/workstreams/WS-PROPHET-CONDITIONAL-FUSION.md
    what: NEW — program workstream, waves w0..w7 chained (w1 depends_on w0, etc. — the
      chain is also what keeps test_agentos_status's unblocked-ranking fixture green)
  - path: agentos/decisions/DEC-PROPHET-ZERO-AUTHORITY-SUPERSEDED-BY-EARNED-CONDITIONAL-AUTHORITY.md
    what: NEW — the CEO ruling as a decision record (DNR keeps kill authority)
  - path: research/DO_NOT_REBUILD.md
    what: KILL-FUSED-COMPOSITE Amendment 3 + KILL-POSITIONING-FUSION Amendment 1,
      arena-scoped, per the registry's same-PR convention
  - path: config/compiled_kill_registry.yml
    what: auto-regenerated by the blocklist hook from the registry edit (same PR by law)
  - path: config/signal_foundry_blocklist.yml
    what: auto-regenerated, same
  - path: engine/neuralweb/context_api.py
    what: "ADDENDUM #5602 — _short_int_dim resolves historical dates PIT against the
      history+panel union gated on knowable_date (= settlement + 10d, drift-pinned to
      scripts/backfill_finra_short_interest.KNOWABLE_LAG_DAYS), basis='pit_settlement';
      historical dates never fall back to the snapshot; current dates keep
      snapshot_not_pit unchanged"
  - path: tests/test_context_api.py
    what: "ADDENDUM #5602 — PIT battery: mid-period resolution, publication-lag honesty,
      pre-history absent, snapshot-fallthrough dead, panel-preference/union, JSON-safety,
      both basis literals pinned, lag-constant drift test"
  - path: tests/test_finra_short_interest_pit.py
    what: "ADDENDUM #5602 — TestAccrualWiredInsideFetch pins the accrual INSIDE
      fetch_short_interest with stubbed network seams (the copy-block tests stay green
      when the block is deleted; this one fails)"
verified:
  - claim: Agent OS store valid with the new records
    command: python3 scripts/agentos.py validate
    result: "39 records — 0 error(s), 0 warning(s)"
  - claim: agentos suites green (incl. the unblocked-ranking fixture the new WS touches)
    command: python -m pytest tests/test_agentos_schema.py tests/test_agentos_status.py tests/test_agentos_compile.py -q
    result: "124 passed (status suite re-run green after the wave depends_on chain)"
  - claim: compiled blocklists consistent with the amended registry
    command: python3 scripts/check_blocklist_drift.py
    result: "OK — no drift detected"
  - claim: no banned validated-claims regression
    command: python3 scripts/check_validated_claims.py
    result: exit 0
  - claim: context-vector store stalled at 2026-08-07 (4 stamped days ever)
    command: read stamp_date sets of data/us_prophet_rank/candidates/*.parquet at HEAD and origin/main (blob-identical)
    result: "{2026-07-31} + {2026-08-05,06,07}; snapshots.jsonl meanwhile advanced to 08-13"
  - claim: the live prophet score has never been graded
    command: intersect prophet-scored snapshot dates {08-07,08-12,08-13} with retro_grades as_of range (06-15..07-31)
    result: empty; data/us_prophet_rank/grades/ absent at HEAD; miss-audit scorecard available:false
  - claim: "ADDENDUM #5602 — short_int PIT resolver + accrual wiring green"
    command: python3 -m pytest tests/test_context_api.py tests/test_finra_short_interest_pit.py -q
    result: "73 passed; 6 mutation checks each caught by a named test; all 11 test files
      matching context_api|context_snapshot green (3 pre-existing test_grade_us_board
      failures are sparse-worktree data/us_board_ledger artifacts, identical at HEAD~)"
  - claim: "ADDENDUM #5602 — history-store accrual healthy and in-lane, NOT orphaned"
    command: read origin/main:data/finra/short_interest_history.parquet; git log --
      data/finra/short_interest_history.parquet; grep fetch_short_interest scripts/build_site.py
    result: "3 settlements (2026-06-30/07-15/07-31 — the chip's '2 settlements' fact was
      stale, 07-15 captured 08-06); writer runs in build_site Phase 3, committed by the
      nightly regime-update commits"
unverified:
  - claim: CI packs green on this docs+agentos+registry diff
    what_would_verify: PR #5593 checks concluding (merge-on-green armed; sweeper owns the merge)
  - claim: the context-vector accrual chip was picked up
    what_would_verify: a diagnosis session/PR exists, or PR-1a executes §13.0 itself
unresolved:
  - "Context-vector accrual stalled since 08-07 — chipped for diagnosis; not fixed here
    (docs-only PR); PR-1a executes §13.0 if the chip was not picked up"
  - "Insider panel collector stopped at 2026q1 — collector repair outside this PR's
    fence, sequenced in §13.0. (The _short_int_dim snapshot_not_pit half of this item
    is FIXED, armed as #5602 — see addendum)"
  - "H=42/63 grades have zero rows, so the basing-class chartered headline is
    indefinitely dark until the grades ladder matures (§8.7) — calendar, not code"
next_actions:
  - "After BOTH PR-1a and #5602 merge: flip the families.yml short_int member's
    pit_status → pit_settlement (resolver basis strings: pit_settlement historical /
    snapshot_not_pit current; #5602 deliberately does not touch families.yml)"
  - "Before any deep historical short_int join: re-run
    scripts/backfill_finra_short_interest.py (resumable, ~20 min) — the panel parquet is
    ABSENT on the primary checkout despite the committed 08-05 coverage JSON; committed
    history depth is 3 settlements (first knowable 2026-07-10)"
  - "PR-1a (wave w1, after #5593 merges): §13 accrual columns; §13.0 accrual restoration
    verified-or-executed; families.yml (memberships + pit_status + coverage_floor +
    max_staleness_sessions); arena harness skeleton with the §9.2 fold-refusal rule"
  - "PR-1b: baseline race G0/G0'/G1/G2/G3/G4 + C1, counterfactual_replay-labelled,
    non-promotion-bearing, §8.7 power table beside it"
  - "Watch ~2026-08-24: first H=10 grade maturation for v1 stamps — the champion's
    first-ever graded read (§6.1); verify priority_score_scorecard flips available:true"
  - "When #5578/#5583 PR-0s merge: add WS:LIVE-ENTRY-RADAR + WS:STOCK-IDENTITY to
    depends_on (they are prose-only references today because their records are unmerged)"
do_not_redo:
  - "Do not re-run the estate census — §2-§6 of the masterplan carry it with receipts;
    re-verify only what a later wave depends on"
  - "Do not re-litigate the 11 review blockers — §17 records each resolution; attack the
    RESOLUTIONS if attacking, not the original draft"
  - "Do not amend further DNR rows for this program — the two amendments landed; the
    eight adjacent rows are dispositioned INTACT in §12"
  - "Do not proxy the Entry head with grades-store MDD/MFE orderings — that stand-in was
    withdrawn as a rival outcome (§7 O4); the head is deferred until LER grades exist"
danger_areas:
  - "context_frame historical short_int__ columns shifted with #5602 (adds
    knowable_date/capture_date/panel flags; drops asof) — zero consumers verified
    2026-08-14; PR-1a memberships must name the live columns"
  - "families.yml is the law when it lands (PR-1a) — the §5.1 prose table is the view;
    keep the uniqueness/pit_status/floor tests in the same PR as the YAML"
  - "The graded frame's era boundaries (§7) + the CV universe-widening break make naive
    pooling silently wrong; every study joins per-stamp-date and per-era"
  - "gh quota: this program's future waves must not poll CI (merge-on-green owns it)"
  - "scripts/build_prophet.py:2185/:2192 stale 'us_prophet_v1' literal — fix rides
    PR-1a; do not fix it in an unrelated heal and orphan the masterplan's §2 note"
---

Cold-stranger summary: read the masterplan top-to-bottom (§0 ruling → §17 review); it is
self-contained. The one live operational fact to check before building anything: has the
context-vector store stamped a night since 2026-08-07? If not, §13.0 outranks everything
else in PR-1a.

Addendum (same day, session claude/si-pit-context-dim, PR #5602): the landmine's
short_int half is repaired — _short_int_dim now resolves historical dates PIT against
the history+panel union on knowable_date (settlement + 10 calendar days, drift-pinned to
the backfill's KNOWABLE_LAG_DAYS), basis='pit_settlement', and returns absent rather
than the snapshot when the stores cannot answer; current dates keep snapshot_not_pit.
Accrual verified healthy and in-lane (3 committed settlements; a wiring test now pins
the accrual inside fetch_short_interest itself). families.yml untouched by design — the
pit_status flip is sequenced in next_actions above.
