---
workstream: "WS:EVAL-OS-MEASUREMENT-LAW"
session: sol/eval-os-program-lead-20260830
model: sol
mission: >
  Adversarially prove that the real qledger readiness/consumer path refuses every illegal
  promotion-evidence shape after the settled E1 qledger-path changes, without creating a new
  promotion authority, score store, cohort plane, ledger, registry, queue, or router.
state_before: >
  E1 worker-recovery child eval-os-e1-thematic-cohort-recovery-20260829-sol-001 is terminally
  Sol-accepted only at APPLIED_REMOTE_DRAFT / BUILT_NOT_PROVEN / PRODUCTION_INERT. Its sole repair
  carrier #6677 is open/unmerged and changes only engine/thematic_desk.py + tests/test_thematic_desk.py;
  it does not change qledger/grader/evidence-clock semantics. stock_desk general clock remains
  PROVEN_LIVE; thematic_desk still has no lawful durable general clock. A general-clock authority-
  cohort filter is not present in current qledger and that semantic gap is explicitly deferred
  outside the E1 child. A1 PR #6651 is active and currently modifies engine/qledger.py and
  scripts/grade_qledger.py, so P1 execution is collision-held until that carrier settles.
operation_key: eval-os-p1-promotion-integrity-acceptance-20260830-sol-001
preferred_avenue: CTO Sol
receiver_binding_mode: CAPACITY_SELECTABLE
placement_state: WAITING_CAPACITY / needs_placement
execution_gate: HELD_COLLISION_A1_PR_6651
why: >
  This is difficult but bounded adversarial engineering/acceptance work over a frozen measurement
  architecture. CTO Sol is the least-scarce specialist avenue that can reliably attack qledger,
  readiness, replay and promotion-boundary behavior.
why_not_fable: >
  The strategic and measurement architecture is already frozen; no principal-level architecture
  ambiguity needs Fable. The remaining task is bounded falsification and proof.
---

# Eval OS P1 — Promotion / clock-laundering adversarial acceptance

This file freezes a **fresh independent P1 scope** after terminal acceptance of the E1 worker-repair
child. It is an organizational scope packet only. It is **not a receiver assignment, START, watcher,
merge/deploy authorization, production mutation, or authority change**.

## Parent transition banked

- H1/T4 remains terminal `PROVEN_LIVE`; never reopen it.
- Old E1 `eval-os-e1-clock-accrual-20260827` remains terminal; never reopen it.
- Fresh E1 recovery `eval-os-e1-thematic-cohort-recovery-20260829-sol-001` is now terminal at the
  worker-repair boundary only. Its effect is banked as `APPLIED_REMOTE_DRAFT / BUILT_NOT_PROVEN /
  PRODUCTION_INERT`.
- Macro PR #6677 is still the sole bounded thematic fail-soft/quorum repair carrier and is not merged
  or deployed by that STOP. The first lawful `thematic_desk` claim + general evidence clock remains a
  separate real-production acceptance gate. Never manufacture either.
- The E1 repair changes no qledger/grader/evidence-clock semantics. Therefore E1's qledger-path
  movement is settled enough to freeze P1's acceptance target.
- The discovered general-clock cohort-authority gap is **deferred outside E1 and outside this P1
  implementation scope**. P1 may expose/falsify the real current consumer contract; it may not invent
  a new general-clock cohort gate or promotion authority to make a desired test pass.

## Current execution hold

P1 must **not START yet**. Active A1 carrier PR #6651 currently changes `engine/qledger.py` and
`scripts/grade_qledger.py` while repairing the Intelligence OS derived evidence view. Those paths are
part of P1's real readiness/grading/consumer surface. Until A1 reaches a terminal Sol disposition and
a fresh path/source census proves the P1 acceptance target stable, starting P1 would create a live
collision and ambiguous ruler.

Routine concrete placement is also not Chairman labor. No current production-proven automatic
quota-bearing placement path is asserted here. Until the accepted placement owner supplies a lawful
receiver (or a current explicit assignment does), this child remains:

```text
PREFERRED_AVENUE: CTO Sol
RECEIVER_BINDING_MODE: CAPACITY_SELECTABLE
PLACEMENT_STATE: WAITING_CAPACITY / needs_placement
```

Do not create a worker-facing Slack commission, OPEN_PICKUP, numbered-account assignment, or
receiver-specific watcher while either gate is held.

## Observable mission

One exact-current acceptance suite and real-path proof demonstrates that illegal evidence cannot be
laundered into readiness/promotion support through the real qledger consumer path.

## Required adversarial mutations

The acceptance work must independently kill all of these through the real readiness/consumer path,
not merely helper functions:

1. legacy evidence treated as new authority;
2. explicit + legacy clock bases pooled;
3. explicit horizon unit or market basis stripped/ignored;
4. a >63-session declared check-by (for example 126 trading days) mislabeled as an own-horizon grade;
5. retrospective registration accepted as prospective;
6. evidence-clock start backdated to thesis/claim `asof` rather than first accepted registration;
7. matched-control policy inferred from which rows happen to contain controls rather than governed policy;
8. missing/unpriceable required control silently removed from the denominator;
9. replay maturity decided from wall clock instead of the canonical replay/session clock;
10. direction sign erased, inverted, or pooled in promotion arithmetic.

A mutation that is impossible to express because the current canonical owner already refuses the
shape may be proven by the owner's existing executable contract plus real-path integration evidence;
do not create a duplicate implementation just to obtain a new test hook.

## Frozen laws / non-goals

- Qledger/readiness remains measurement/advisory evidence; existing qual-ladder/species/prereg/
  gauntlet owners remain promotion authority.
- No second claims/grades store, evidence clock store, score database, promotion service, cohort plane,
  engine registry, queue, router, or replay framework.
- Legacy and explicit bases may be displayed side-by-side but never pooled.
- Market/horizon unit/ruler remain part of measurement identity.
- >63 declared horizons do not gain new own-horizon grades under this program.
- `stock_desk` and `demand_chain` remain matched-control-required; `thematic_desk` remains benchmark-only.
- Missing required controls reduce coverage; they do not leave the denominator.
- General evidence-clock registration time is first accepted prospective registration, never thesis time.
- Do not silently reinterpret the deferred general-clock authority-cohort gap as already solved. If a
  P1 acceptance claim genuinely requires a semantic gate that current authority does not contain,
  return `DECISION_REQUEST / OWNER_SEMANTIC_GAP` with exact evidence instead of modifying authority.

## Launch gate

Before any receiver assignment or START, Sol must freshly reconcile:

1. CURRENT protected Mastermind Skillpack from one exact SHA;
2. CURRENT Macro main and this recovery architecture;
3. terminal state of A1 PR #6651 / its recovery child and whether its qledger/grader path changes
   have landed, been rejected, or remain active;
4. PR #6677 / E1 parent production state only as evidence, without reopening the terminal worker child;
5. open PR/path collisions on qledger, qledger_validity, grade_qledger, readiness/promotion consumers,
   replay clock and control-policy paths;
6. lawful current placement from the accepted capacity/continuity owner or an explicit current receiver assignment.

Any active collision or effect uncertainty keeps P1 held.

## Completion / return

P1 is complete only when the final exact-head/current-base suite proves the required mutations fail
closed through the real consumer path, independent review finds no duplicate authority/control plane,
and all inherited/foreign failures are separately attributed. Production/browser evidence is required
where the acceptance claim concerns a deployed consumer rather than a pure contract.

Return exact head, files, mutation matrix, CI/fences/security, independent review, real-path receipts,
foreign/inherited reds, owner-semantic gaps, and the exact next parent gate. Only a separate explicit
Sol STOP may terminalize the future execution child.
