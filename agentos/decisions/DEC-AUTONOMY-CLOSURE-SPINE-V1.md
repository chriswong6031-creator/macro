---
schema: agentos.decision.v1
key: AUTONOMY-CLOSURE-SPINE-V1
question: >
  How should Mastermind close the remaining autonomy-composition gap without replacing existing
  lifecycle, action-target, Wake, dialogue, capacity, COO, Control Room, or Agent OS owners?
answer: >
  Adopt Autonomy Closure Spine v1 and preserve the existing W3C -> C2 -> MAT-S1 -> Stage-B1 ->
  Control Room -> golden-root train. Authorize only ACF-1 Semantic Directive Convergence before
  fleet proof. ACF-1 uses one Executive Runtime command domain per exact terminal-return revision;
  reuses control_plane.sol_action_target.require_sol_action_authority as the sole v1 ACTION_TARGET
  actor owner; validates one exact bounded CONTINUE/REPAIR/STOP/ESCALATE body; canonicalizes through
  the existing control_plane.wake_events.canonical_json_bytes owner; commits one immutable Event;
  and lets the existing COO cycle consume it once in the same transaction as the downstream
  mutation. Fresh Chairman intervention remains human/session authority and is submitted by the
  then-current exact action target as revision N+1. ACF-2 through ACF-6 remain evidence-gated.
rationale: >
  Target identity and transport truth do not establish which semantic decision became effective.
  Live W3C and C2 carriers demonstrated conflicting or stale action-looking Sol instructions. Slack
  ordering cannot atomically reject an observer, stale target, changed body, or CONTINUE/STOP race.
  Including actor, target generation, or decision in the command key would let rivals mint parallel
  command IDs. A free-form body would become an undeclared model-authored command language. A direct
  CHAIRMAN Runtime actor would invent an authority owner that does not currently exist. The smallest
  lawful repair is one return-revision conflict key, one exact body union, one existing action-target
  authority check, one Runtime Event, and one existing-COO consumer.
alternatives:
  - option: Finish every existing component independently and integrate later
    why_not: Locally correct components can still accept conflicting semantic decisions.
  - option: Build a new generic autonomy platform
    why_not: It would duplicate Executive lifecycle, Event, routing, retry, identity, and state owners.
  - option: Treat latest Slack prose as the effective directive
    why_not: Slack is transport and cannot transactionally bind return, actor, conflict, or consumption.
  - option: Use actor, target generation, or decision in command identity
    why_not: Rival proposals would derive different command IDs and could all commit.
  - option: Use control_plane.ceo_intent.canonical_json_bytes
    why_not: That symbol does not exist; the protected existing owner is control_plane.wake_events.canonical_json_bytes.
  - option: Add a direct machine-authenticated Chairman actor in v1
    why_not: No protected Runtime principal/receipt owner supports it.
evidence:
  - "Mastermind issue #437 is the single Chairman-approved architecture carrier."
  - "Mastermind PR #438 remains the exact three-path F0 source carrier on protected base 7022e70640637a4fa07f073442dc693301290e2a."
  - "Current F0 head f8aa76ec389db7109da4273836423e2c32fc98f8 repairs the nonexistent ceo_intent canonicalizer and reuses control_plane.wake_events.canonical_json_bytes."
  - "The current contract accepts exactly ACTION_TARGET through mastermind.sol_action_target.v1 and SolActionTargetResolution.evidence_digest."
  - "The exact body schema is mastermind.executive_semantic_directive_body/v1 with normalized CommissionRef for REPAIR and closed STOP/ESCALATE reasons."
  - "Mastermind PR #427 protected W3C source through a945e76befb34d15d0ab0e369b4197901883bb16."
  - "Mastermind PR #415 remains the active C2-R1A Runtime writer and gates ACF-1 implementation."
  - "Mastermind PR #326 remains the active Control Room writer."
  - "Issue #400 is closed duplicate; issue #386 remains canonical."
affects:
  - WS:CHAIRMAN-CONTROL-ROOM
  - WS:EXECUTIVE-CAPACITY-FABRIC
  - Mastermind#386
  - Mastermind#437
  - Mastermind#438
confidence: high
reversibility: easy
decided_by: chairman
decided_at: 2026-09-03
---

# Autonomy Closure Spine v1

ACF-1 is the only newly authorized pre-fleet closure layer and remains
`WAITING_ARCHITECTURE_PROTECTION / WAITING_RUNTIME_PATH_RELEASE / needs_placement`.
No implementation worker, branch, Runtime Event, provider action, deployment, or canary exists.

## Existing-owner boundary

- Executive Runtime owns command conflict, Event commit/readback, and lifecycle truth.
- `control_plane.sol_action_target` plus SessionTargetRegistry/RuntimeBindingSnapshot owns the sole v1 actor authority.
- `control_plane.wake_events.canonical_json_bytes` owns canonical JSON bytes.
- `common.commission_ref` owns immutable REPAIR source identity.
- terminal-return owns exact source completion evidence.
- W3C/Wake/Agent Relay own attention and delivery; Agent Dialogue owns transport.
- the existing COO cycle owns once-only consumption and its ordinary downstream Event receipt.
- Control Room may later project state but never author it.

Consumed, applied, or effect-unknown work cannot be reversed by later prose. ACF-2 through ACF-6 may
start only when golden-root evidence proves a concrete existing-owner gap.
