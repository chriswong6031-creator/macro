# Compact risk context — current evidence

This is LOCAL canonical-target/browser proof, not deployment or production authentication.
The final externalized macro.html SHA256 is `926f49e528efa01b63c58dd1eca8cf3458a29db15a1817b1aa83ad1ff85437ae`.
`canonical-target-receipt.json` binds the pre-externalization target; `browser-receipt.json` and `live-context-receipt.json` bind final bytes.

## Design baseline and purpose
Continue the original PR's compact integrated rail, not a second hero. Preserve the existing main Risk Brief and date. State at a glance; the existing Risk Detail destination supplies inspection and the nested evidence disclosure supplies receipts.
DARK TREATMENT: a quiet panel2 wash and hairline join into the hero, with subdued metadata and distinct directional/hazard inks; no new glow or giant warning card.
LIGHT TREATMENT: a pale rail against white hero material and a visible hairline against the cool canvas, dark text, muted dates and legible non-neon hazard ink. It uses the existing research-workspace materials, not a duplicated runtime palette.
The information architecture, tap target, keyboard/focus behavior and source meanings are identical. Chinese directional red/green follows existing design tokens; hazard severity does not invert with locale.

## Evidence
`home-{1440,768,390}-{dark,light}-{en,zh}.png`: twelve real Chromium views, no horizontal page overflow; market ticker remains above 820px on desktop.
`detail-*`: the existing dialog with explanation opened, no replacement overlay/controller.
`live-{healthy,degraded,stale}-390.png`: explicit feed fixtures running the unchanged actual live painter. The settled date stays visible. These are not real open-session market events.
`owner-tests-resumed.log`: 289 owning/adjacent tests passed after session recovery.
`presentation-final.log`: focused contract verification after restoring timestamp-only generated input churn.
The original full-site builder was stopped after its macro target while processing unrelated stock pages. `canonical-build.log` is historical diagnostic evidence, never a full-site success receipt.
The target harness calls the actual builder, data composition, Jinja environment and page writer, then exits at the macro target. `externalize.log` records the normal externalization pass. No manual HTML splice is used for this candidate.

## Remaining release gates
Independent exact-head semantic and visual review, current hosted integration checks, explicit Sol release on the existing PR, normal publication, and live browser verification.
Local dialog tests intentionally stub anonymous status. They do not change product auth or claim that a production member session was tested.
