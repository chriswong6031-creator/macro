(function(global){
  'use strict';
  global.createGovernmentRevenueDossier=function(api){
    var obj=api.obj,arr=api.arr,esc=api.esc,text=api.text,n=api.n,money=api.money,date=api.date,tr=api.tr,safeUrl=api.safeUrl,factCell=api.factCell,hostFor=api.host,getSelected=api.selected,epoch=0,session=null;
    function field(x,keys){x=obj(x)?x:{};for(var i=0;i<keys.length;i++)if(x[keys[i]]!=null&&x[keys[i]]!=='')return x[keys[i]];return null}
    function fetchPage(route){if(typeof global.fetch!=='function')return Promise.reject(new Error('unavailable'));return global.fetch('/api/government-revenue/'+route,{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(r){if(!r.ok)throw new Error('http_'+r.status);return r.json()}).then(function(x){if(!obj(x)||x.schema_version!=='government_revenue_dossiers.v1'||!/^grd1-[a-f0-9]{24}$/.test(text(x.content_id,'')))throw new Error('contract');return x})}
    function name(a){return text(field(a,['description'])||field(obj(a.program)?a.program:{},['name','description','title'])||field(a,['piid','award_key']),tr('Official award record','官方授标记录'))}
    function recipient(a){var x=obj(a.recipient)?a.recipient:{};return text(field(x,['name','legal_name'])||field(a,['recipient_name']),tr('Recipient not reported','未报告收款方'))}
    function identity(a){return obj(a.identity)?a.identity:{}}
    function agency(a){var x=obj(a.agency)?a.agency:{};return text(field(x,['awarding_subagency','awarding','funding_subagency','funding','office_name','subagency_name','department_name','name'])||field(a,['awarding_agency']),tr('Agency not reported','未报告机构'))}
    function amount(a,key){return field(obj(a.values)?a.values:{},[key])}
    function cards(rows){return arr(rows).filter(obj).map(function(a){var id=identity(a);return'<button type="button" class="award-card" data-award-key="'+esc(text(a.award_key,''))+'"><span><strong>'+esc(name(a))+'</strong><small>'+esc(text(id.piid,id.generated_award_id))+' · '+esc(agency(a))+'</small></span><span class="award-money">'+esc(money(amount(a,'obligated')))+'<small>'+esc(tr('obligated','已承诺义务'))+'</small></span></button>'}).join('')}
    function bind(host){host.querySelectorAll('[data-award-key]').forEach(function(b){b.addEventListener('click',function(){loadAward(b.dataset.awardKey)})});var more=host.querySelector('[data-more-awards]');if(more)more.addEventListener('click',moreAwards);var back=host.querySelector('[data-dossier-back]');if(back)back.addEventListener('click',renderBook);var actions=host.querySelector('[data-more-actions]');if(actions)actions.addEventListener('click',moreActions)}
    function renderBook(){var s=session,host=hostFor();if(!s||!host)return;var c=s.company||{},coverage=obj(s.list.source_coverage)?s.list.source_coverage:{},fresh=text((s.list.freshness||{}).status,'unavailable'),scope=text(coverage.scope,tr('bounded official award-detail sample','受限的官方授标明细样本')),shown=s.awards.length,total=n(s.list.total);host.innerHTML='<div class="dossier-status"><span><b>'+esc(tr('Official award book','官方授标档案'))+'</b><span>'+esc(scope)+' · '+esc(tr('source state ','来源状态 ')+fresh)+'</span></span><span class="dossier-count">'+esc(shown+' / '+text(total,shown))+'</span></div>'+(shown?cards(s.awards):'<div class="dossier-empty">'+esc(tr('No official award record is visible for this company in the bounded source cut.','当前受限来源截点中没有该公司的可见官方授标记录。'))+'</div>')+(s.next?'<button class="tool-btn" type="button" data-more-awards>'+esc(tr('Load more awards','加载更多授标'))+'</button>':'')+'<div class="limit-copy">'+esc(tr('Stable award identity prefers the generated USAspending award ID. A PIID alone never collapses separate awards.','稳定授标身份优先使用 USAspending 生成授标 ID。仅凭 PIID 绝不会合并不同授标。'))+(c.action_count!=null?' '+esc(tr('Covered actions: ','覆盖行动：')+c.action_count)+'.':'')+'</div>';bind(host)}
    function loadCompany(ticker){var host=hostFor(),ticket=++epoch;ticker=text(ticker,'');if(!host||!/^[A-Z][A-Z0-9.-]{0,9}$/.test(ticker))return;host.innerHTML='<div class="dossier-status"><span><b>'+esc(tr('Opening official award book','正在打开官方授标档案'))+'</b><span>'+esc(tr('Loading bounded award records and source coverage…','正在加载受限授标记录与来源覆盖…'))+'</span></span><span class="dossier-count">•••</span></div>';Promise.all([fetchPage('dossier/company/'+encodeURIComponent(ticker)),fetchPage('company/'+encodeURIComponent(ticker)+'/awards?limit=8')]).then(function(x){if(ticket!==epoch||getSelected()!=='company:'+ticker)return;if(x[0].content_id!==x[1].content_id)throw new Error('generation_mismatch');session={ticker:ticker,content_id:x[0].content_id,company:x[0].company||{},list:x[1],awards:arr(x[1].results).filter(obj),next:x[1].next_cursor,award:null,actions:[],actionNext:null};renderBook()}).catch(function(){if(ticket===epoch&&host)host.innerHTML='<div class="dossier-error"><strong>'+esc(tr('Award book unavailable','授标档案不可用'))+'</strong><br>'+esc(tr('The compact company context remains visible. No dossier row was reconstructed in the browser.','精简公司情境仍然可见。浏览器未重建任何档案记录。'))+'</div>'})}
    function moreAwards(){var s=session,host=hostFor();if(!s||!s.next||!host)return;fetchPage('company/'+encodeURIComponent(s.ticker)+'/awards?limit=8&cursor='+encodeURIComponent(s.next)).then(function(page){if(!session||page.content_id!==s.content_id)throw new Error('generation_mismatch');s.awards=s.awards.concat(arr(page.results).filter(obj));s.next=page.next_cursor;renderBook()}).catch(function(){host.insertAdjacentHTML('beforeend','<div class="dossier-error">'+esc(tr('The next award page could not be verified.','下一页授标记录无法核验。'))+'</div>')})}
    function actionRows(rows){return arr(rows).filter(obj).map(function(a){var effective=field(a,['effective_at','action_date']),known=field(a,['known_at']),value=field(a,['obligation','federal_action_obligation','obligation_delta','amount']);return'<div class="action-row"><time>'+esc(date(effective))+'</time><span><strong>'+esc(text(field(a,['action_type_description','description','modification_number','action_id']),tr('Official action','官方行动')))+'</strong><span class="dual-clock">'+esc(tr('Effective ','生效 ')+text(effective)+' · '+tr('known ','获知 ')+text(known))+'</span></span><b>'+esc(money(value))+'</b></div>'}).join('')}
    function renderAward(){var s=session,host=hostFor(),a=s&&s.award;if(!s||!host||!a)return;var id=identity(a),dates=obj(a.dates)?a.dates:{},source=obj(a.source)?a.source:{},src=safeUrl(source.award_page_url||source.award_detail_url||source.award_search_url);host.innerHTML='<div class="dossier-detail"><div class="dossier-nav"><button class="tool-btn" type="button" data-dossier-back>← '+esc(tr('Award book','授标档案'))+'</button><span class="truth official">'+esc(tr('Official record','官方记录'))+'</span></div><h3>'+esc(name(a))+'</h3><div class="dossier-detail-id">'+esc(text(id.generated_award_id,a.award_key))+'</div><div class="inspect-meta">'+esc(recipient(a))+' · '+esc(agency(a))+'</div><div class="semantic-values"><div><span>'+esc(tr('Obligated','已承诺义务'))+'</span><b>'+esc(money(amount(a,'obligated')))+'</b></div><div><span>'+esc(tr('Current value','当前价值'))+'</span><b>'+esc(money(amount(a,'current_award_value')))+'</b></div><div><span>'+esc(tr('Potential ceiling','潜在上限'))+'</span><b>'+esc(money(amount(a,'ceiling')))+'</b></div></div><div class="facts-grid" style="margin-top:9px">'+factCell(tr('Effective at','生效于'),date(field(dates,['effective_at','start_date'])))+factCell(tr('Known at','获知于'),date(field(dates,['known_at'])))+factCell(tr('Current POP end','当前履约结束'),date(field(dates,['end_date'])))+factCell(tr('Award / PIID','授标 / PIID'),text(id.piid))+'</div>'+(src?'<a class="source-link" href="'+esc(src)+'" target="_blank" rel="noopener"><b>'+esc(tr('Open official award','打开官方授标'))+'</b><span>↗</span></a>':'')+'<div class="inspect-label" style="margin-top:13px">'+esc(tr('Official action tape','官方行动脉搏'))+'</div><div class="action-tape">'+(s.actions.length?actionRows(s.actions):'<div class="dossier-empty">'+esc(tr('No action row is visible in this source cut.','当前来源截点中没有可见行动记录。'))+'</div>')+'</div>'+(s.actionNext?'<button class="tool-btn" type="button" data-more-actions style="margin-top:8px">'+esc(tr('Load more actions','加载更多行动'))+'</button>':'')+'<div class="limit-copy" style="margin-top:8px">'+esc(tr('Obligation, current value and potential ceiling are distinct official amount semantics. None is GAAP backlog or reported revenue.','义务额、当前价值与潜在上限是不同的官方金额语义，均不等于 GAAP 积压或报告收入。'))+'</div></div>';bind(host)}
    function loadAward(key){var s=session,host=hostFor();if(!s||!host||!key)return;host.innerHTML='<div class="dossier-status"><span><b>'+esc(tr('Opening award history','正在打开授标历史'))+'</b><span>'+esc(tr('Verifying record and action generation…','正在核验记录与行动数据代次…'))+'</span></span><span class="dossier-count">•••</span></div>';var route='award/'+encodeURIComponent(key);Promise.all([fetchPage(route),fetchPage(route+'/actions?limit=20')]).then(function(x){if(!session||x[0].content_id!==s.content_id||x[1].content_id!==s.content_id)throw new Error('generation_mismatch');s.award=x[0].award;s.actions=arr(x[1].results).filter(obj);s.actionNext=x[1].next_cursor;renderAward()}).catch(function(){host.innerHTML='<div class="dossier-error">'+esc(tr('This award detail could not be verified against the active dossier generation.','该授标明细无法与当前档案代次完成核验。'))+'</div><button class="tool-btn" type="button" data-dossier-back style="margin-top:8px">← '+esc(tr('Award book','授标档案'))+'</button>';bind(host)})}
    function moreActions(){var s=session;if(!s||!s.award||!s.actionNext)return;fetchPage('award/'+encodeURIComponent(s.award.award_key)+'/actions?limit=20&cursor='+encodeURIComponent(s.actionNext)).then(function(page){if(!session||page.content_id!==s.content_id)throw new Error('generation_mismatch');s.actions=s.actions.concat(arr(page.results).filter(obj));s.actionNext=page.next_cursor;renderAward()}).catch(function(){var host=hostFor();if(host)host.insertAdjacentHTML('beforeend','<div class="dossier-error">'+esc(tr('The next action page could not be verified.','下一页行动记录无法核验。'))+'</div>')})}
    return{loadCompany:loadCompany,invalidate:function(){epoch++;session=null}};
  };
})(window);
