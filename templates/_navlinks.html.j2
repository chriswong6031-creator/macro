{# ===========================================================================
   SINGLE SOURCE OF TRUTH for the site navigation menu.

   Every page (both the .site-nav grid family AND the self-contained .topbar
   "vector" family) includes this partial in place of a hand-copied
   <div class="nav-links"> block, so the menu can never drift between pages
   again. The surrounding chrome (the <nav>/<div class="topbar"> wrapper, the
   search box, the theme/lang controls) stays per-page; only the link list is
   shared.

   • Self-contained t() macro — so it works whether or not the host page
     defines its own (an {% include %} does not import the parent's macros).
   • nav_prefix — optional. Pages rendered one directory deep (site/sectors/*)
     set {% set nav_prefix = '../' %} before including so every link resolves
     from the site root (a bare href="macro.html" would 404 as /sectors/...).
   • No per-page `active` class — theme.js highlights the current page (and its
     parent dropdown) by matching the URL, so highlighting stays consistent.

   Structure: Home · United States · China · Hong Kong · Canada · International ·
   Other Assets (3-tier: Bitcoin Vector ▸ / Commodities ▸ fly out; Forex, Bonds
   are leaves) · Research.
   =========================================================================== #}
{% macro t(en, zh='') -%}
<span class="l-en">{{ en|safe }}</span><span class="l-zh">{{ (zh if zh else en)|safe }}</span>
{%- endmacro %}
{% set NP = nav_prefix if (nav_prefix is defined and nav_prefix) else '' %}
    <div class="nav-links">
    {# Brand mark — the crafted Mastermind “M” logomark (gradient tile + ascending
       market-peak M), first item in the menu, links Home. Icon-only (styled by
       .site-nav .nav-brand in theme.css). Kept in sync with the landing-hub lockup
       in scripts/build_vector.py (_BRAND_MARK_SVG). Plain-text aria-label only — the
       bilingual t() macro emits <span> HTML that would corrupt the attribute. #}
    <a class="nav-brand" href="{{ NP }}index.html" aria-label="MASTERMIND — Home">
      <svg class="brand-glyph" width="29" height="29" viewBox="0 0 40 40" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <defs>
          <linearGradient id="mbTile" x1="0" y1="0" x2="1" y2="1">
            <stop offset="0" stop-color="#5b9dff"/><stop offset=".42" stop-color="#3b82f6"/>
            <stop offset=".74" stop-color="#6366f1"/><stop offset="1" stop-color="#7c5cff"/></linearGradient>
          <linearGradient id="mbSheen" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0" stop-color="#ffffff" stop-opacity=".34"/><stop offset=".55" stop-color="#ffffff" stop-opacity="0"/></linearGradient>
          <radialGradient id="mbGlow" cx=".5" cy=".4" r=".65">
            <stop offset="0" stop-color="#ffffff" stop-opacity=".22"/><stop offset="1" stop-color="#ffffff" stop-opacity="0"/></radialGradient>
          <linearGradient id="mbInk" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0" stop-color="#ffffff"/><stop offset="1" stop-color="#dbe7ff"/></linearGradient>
        </defs>
        <rect x="3" y="3" width="34" height="34" rx="10.5" fill="url(#mbTile)"/>
        <rect x="3" y="3" width="34" height="34" rx="10.5" fill="url(#mbGlow)"/>
        <rect x="3" y="3" width="34" height="34" rx="10.5" fill="url(#mbSheen)"/>
        <rect x="3.7" y="3.7" width="32.6" height="32.6" rx="9.9" fill="none" stroke="#ffffff" stroke-opacity=".28"/>
        <g fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="3.3">
          <path d="M13 28 L13 14.5 L20 22 L27 12.5 L27 28" stroke="#15205a" stroke-opacity=".30" transform="translate(0,1.1)"/>
          <path d="M13 28 L13 14.5 L20 22 L27 12.5 L27 28" stroke="url(#mbInk)"/>
        </g>
      </svg>
      <span class="brand-word">MASTERMIND</span>
    </a>
    {# Home link removed — the brand logomark above links Home (standard pattern). #}
    <div class="nav-dd">
      <a class="nav-link" href="{{ NP }}macro.html">🇺🇸 {{ t('United States', '美国') }} <span class="caret">▾</span></a>
      <div class="nav-dd-menu" role="menu">
        <a href="{{ NP }}macro.html">📊 {{ t('Macro Dashboard', '宏观仪表盘') }}<span class="d">{{ t('Regime · growth · inflation · nowcast', '周期 · 增长 · 通胀 · 即时预测') }}</span></a>
        <a href="{{ NP }}news.html">📰 {{ t('News Feed', '新闻流') }}<span class="d">{{ t('Macro headlines · catalysts · sentiment', '宏观头条 · 日程 · 情绪') }}</span></a>
        <a href="{{ NP }}us_stocks.html">📈 {{ t('Stock Dashboard', '股票仪表盘') }}<span class="d">{{ t('Standouts · sectors · flows', '精选 · 板块 · 资金') }}</span></a>
        <a href="{{ NP }}sector_heatmap.html">🔥 {{ t('Sector Heatmap', '板块热力图') }}<span class="d">{{ t('All 503 names · market-cap treemap · click for our read', '全部503只 · 市值树图 · 点击查看研判') }}</span></a>
        <a href="{{ NP }}baskets.html">🧺 {{ t('Thematic Baskets', '主题篮子') }}<span class="d">{{ t('Equal-weight theme baskets vs S&P', '等权主题篮子 vs 标普') }}</span></a>
        <a href="{{ NP }}allocation.html">🔄 {{ t('Narrative Rotation', '主题轮动') }}<span class="d">{{ t('Which theme to hold & when to rotate', '持有哪个主题 · 何时轮动') }}</span></a>
        <a href="{{ NP }}news.html">🗞️ {{ t('News', '新闻') }}<span class="d">{{ t('Macro · markets · sectors · baskets · China', '宏观 · 大盘 · 板块 · 篮子 · 中国') }}</span></a>
        <a href="{{ NP }}whitehouse.html">🏛️ {{ t('White House Watch', '白宫快讯') }}<span class="d">{{ t('Policy alerts · sector head/tailwinds · exposed names', '政策快讯 · 行业顺逆风 · 受影响标的') }}</span></a>
        <a href="{{ NP }}special_situations.html">⚡ {{ t('Special Situations', '特殊事件') }}<span class="d">{{ t('Activist · M&A · spin-offs · take-privates', '维权 · 并购 · 分拆 · 私有化') }}</span></a>
        <a href="{{ NP }}strategies.html">🎛️ {{ t('Strategies', '策略') }}<span class="d">{{ t('Tactical strategy scorecards', '战术策略记分卡') }}</span></a>
        <a href="{{ NP }}ipo.html">🆕 {{ t('IPO Radar', '新股雷达') }}<span class="d">{{ t('IPO window · aftermarket reality', '新股窗口 · 二级市场现实') }}</span></a>
        <a href="{{ NP }}alerts.html">🚨 {{ t('Alert Center', '警报中心') }}<span class="d">{{ t('Ranked & triaged alerts', '分级分诊的警报') }}</span></a>
        <a href="{{ NP }}gex.html">🧲 {{ t('Options Desk', '期权台') }}<span class="d">{{ t('GEX · walls · vol surface', 'GEX · 墙 · 波动率曲面') }}</span></a>
      </div>
    </div>
    <div class="nav-dd">
      <a class="nav-link" href="{{ NP }}china.html">🇨🇳 {{ t('China', '中国') }} <span class="caret">▾</span></a>
      <div class="nav-dd-menu" role="menu">
        <a href="{{ NP }}china.html">📊 {{ t('Macro Dashboard', '宏观仪表盘') }}<span class="d">{{ t('A-share regime · sectors · flows', 'A股周期 · 板块 · 资金') }}</span></a>
        <a href="{{ NP }}china_news.html">📰 {{ t('China News Feed', '中国新闻流') }}<span class="d">{{ t('Policy tone · official sources · market tags', '政策基调 · 官方来源 · 市场标签') }}</span></a>
        <a href="{{ NP }}china_stocks.html">📈 {{ t('Stock Dashboard', '股票仪表盘') }}<span class="d">{{ t('Standouts · alpha · setups', '精选 · 阿尔法 · 布局') }}</span></a>
        <a href="{{ NP }}china_sector_desk.html">🧭 {{ t('Sector Desk', '行业桌面') }}<span class="d">{{ t('GS-style sector indices · live · pathway', 'GS式行业指数 · 实时 · 路径') }}</span></a>
        <a href="{{ NP }}baskets_china.html">🧺 {{ t('Thematic Baskets', '主题篮子') }}<span class="d">{{ t('Equal-weight theme baskets vs CSI 300', '等权主题篮子 vs 沪深300') }}</span></a>
        <a href="{{ NP }}allocation_china.html">🔄 {{ t('Narrative Rotation', '主题轮动') }}<span class="d">{{ t('Which theme to hold & when to rotate', '持有哪个主题 · 何时轮动') }}</span></a>
        <a href="{{ NP }}china_strategies.html">🎛️ {{ t('Strategies', '策略') }}<span class="d">{{ t('Tactical strategy scorecards', '战术策略记分卡') }}</span></a>
        <a href="{{ NP }}china_news.html">📰 {{ t('News & Intelligence', '新闻与情报') }}<span class="d">{{ t('Multi-source news tape · media sentiment', '多源新闻脉搏 · 媒体情绪') }}</span></a>
        <a href="{{ NP }}china_policy_watch.html">🏛️ {{ t('PBoC & Policy Watch', '央行与政策观察') }}<span class="d">{{ t('Rate corridor · liquidity · sector policy', '利率走廊 · 流动性 · 行业政策') }}</span></a>
        <a href="{{ NP }}china_altdata.html">🛰️ {{ t('Alternative Data', '另类数据') }}<span class="d">{{ t('Smart-money convergence · signal lab', '主力共振 · 信号实验室') }}</span></a>
        <a href="{{ NP }}china_radar.html">📡 {{ t('Divergence Radar', '背离雷达') }}<span class="d">{{ t('Policy/flow signal vs sector price', '政策/资金信号 vs 板块价格') }}</span></a>
        <a href="{{ NP }}china_intel.html">🧠 {{ t('Intelligence Hub', '情报中心') }}<span class="d">{{ t('All China intel + Mastermind feed', '全部中国情报 + Mastermind信号') }}</span></a>
      </div>
    </div>
    <div class="nav-dd">
      <a class="nav-link" href="{{ NP }}hk.html">🇭🇰 {{ t('Hong Kong', '香港') }} <span class="caret">▾</span></a>
      <div class="nav-dd-menu" role="menu">
        <a href="{{ NP }}hk.html">📊 {{ t('Macro Dashboard', '宏观仪表盘') }}<span class="d">{{ t('HSI · southbound · global risk', 'HSI · 南向 · 全球风险') }}</span></a>
        <a href="{{ NP }}hk_stocks.html">📈 {{ t('Stock Dashboard', '股票仪表盘') }}<span class="d">{{ t('HK-listed standouts · alpha', 'HK上市精选 · 阿尔法') }}</span></a>
        <a href="{{ NP }}baskets_hk.html">🧺 {{ t('Thematic Baskets', '主题篮子') }}<span class="d">{{ t('Equal-weight theme baskets vs HSI', '等权主题篮子 vs 恒指') }}</span></a>
        <a href="{{ NP }}allocation_hk.html">🔄 {{ t('Narrative Rotation', '主题轮动') }}<span class="d">{{ t('Which theme to hold & when to rotate', '持有哪个主题 · 何时轮动') }}</span></a>
      </div>
    </div>
    <div class="nav-dd">
      <a class="nav-link" href="{{ NP }}canada.html">🇨🇦 {{ t('Canada', '加拿大') }} <span class="caret">▾</span></a>
      <div class="nav-dd-menu" role="menu">
        <a href="{{ NP }}canada.html">📊 {{ t('Macro Dashboard', '宏观仪表盘') }}<span class="d">{{ t('TSX · BoC · commodity overlay', 'TSX · 加央行 · 商品叠加') }}</span></a>
        <a href="{{ NP }}canada_stocks.html">📈 {{ t('Stock Dashboard', '股票仪表盘') }}<span class="d">{{ t('TSX standouts · sectors · alpha', 'TSX精选 · 板块 · 阿尔法') }}</span></a>
        <a href="{{ NP }}baskets_canada.html">🧺 {{ t('Thematic Baskets', '主题篮子') }}<span class="d">{{ t('Equal-weight theme baskets vs S&P/TSX', '等权主题篮子 vs 标普/多伦多') }}</span></a>
        <a href="{{ NP }}allocation_canada.html">🔄 {{ t('Narrative Rotation', '主题轮动') }}<span class="d">{{ t('Which theme to hold & when to rotate', '持有哪个主题 · 何时轮动') }}</span></a>
      </div>
    </div>
    <div class="nav-dd">
      <a class="nav-link" href="{{ NP }}intl.html">🌍 {{ t('International', '国际') }} <span class="caret">▾</span></a>
      <div class="nav-dd-menu" role="menu">
        <a href="{{ NP }}intl.html">📊 {{ t('Macro Dashboard', '宏观仪表盘') }}<span class="d">{{ t('JP · KR · TW · UK · EU compared', '日 · 韩 · 台 · 英 · 欧 对比') }}</span></a>
        <a href="{{ NP }}intl_stocks.html">📈 {{ t('Stock Dashboard', '股票仪表盘') }}<span class="d">{{ t('Cross-market standouts · sectors', '跨市场精选 · 板块') }}</span></a>
        <a href="{{ NP }}baskets_intl.html">🧺 {{ t('Thematic Baskets', '主题篮子') }}<span class="d">{{ t('Cross-country theme baskets · ex-US', '跨国主题篮子 · 除美') }}</span></a>
      </div>
    </div>
    <div class="nav-dd">
      <a class="nav-link" href="javascript:void(0)">💠 {{ t('Other Assets', '其他资产') }} <span class="caret">▾</span></a>
      <div class="nav-dd-menu" role="menu">
        <div class="nav-dd nav-sub">
          <a class="nav-sub-trig" href="{{ NP }}vector.html">₿ {{ t('Bitcoin Vector', '比特币向量') }} <span class="caret-r">▸</span></a>
          <div class="nav-dd-menu" role="menu">
            <a href="{{ NP }}vector.html">📊 {{ t('Overview', '总览') }}<span class="d">{{ t('Market state · risk · momentum', '市场状态 · 风险 · 动量') }}</span></a>
            <a href="{{ NP }}vector_allocation.html">◎ {{ t('Allocation Strategy', '仓位策略') }}<span class="d">{{ t('BTC · ETH · alts · cash', 'BTC · ETH · 山寨 · 现金') }}</span></a>
          </div>
        </div>
        <div class="nav-dd nav-sub">
          <a class="nav-sub-trig" href="{{ NP }}commodities.html">🛢 {{ t('Commodities', '大宗商品') }} <span class="caret-r">▸</span></a>
          <div class="nav-dd-menu" role="menu">
            <a href="{{ NP }}commodities.html">📊 {{ t('Commodity Dashboard', '商品仪表盘') }}<span class="d">{{ t('Gold · silver · copper · oil · complex', '金 · 银 · 铜 · 油 · 综合') }}</span></a>
            <a href="{{ NP }}commodity_strategies.html">🎛️ {{ t('Strategies', '策略') }}<span class="d">{{ t('Per-commodity tactical strategies', '分品种战术策略') }}</span></a>
            <a href="{{ NP }}spr.html">🛢️ {{ t('Strategic Reserves', '战略储备') }}<span class="d">{{ t('SPR levels by country vs oil prices', '各国战略储备水平与油价') }}</span></a>
          </div>
        </div>
        <a href="{{ NP }}forex.html">💱 {{ t('Forex', '外汇') }}<span class="d">{{ t('Dollar desk · G10 carry · pairs', '美元桌 · G10套息 · 货币对') }}</span></a>
        <a href="{{ NP }}bonds.html">🏛️ {{ t('Bonds', '债券') }}<span class="d">{{ t('Curve · credit · duration compass', '曲线 · 信用 · 久期罗盘') }}</span></a>
      </div>
    </div>
    <div class="nav-dd">
      <a class="nav-link" href="javascript:void(0)">🔬 {{ t('Research', '研究') }} <span class="caret">▾</span></a>
      <div class="nav-dd-menu" role="menu">
        <a href="{{ NP }}intelligence_hub.html">🛰️ {{ t('Intelligence Hub', '情报中心') }}<span class="d">{{ t('Central command · all desks fused · 2nd/3rd-order reads', '中枢指挥 · 所有信号台融合 · 二三阶推理') }}</span></a>
        <a href="{{ NP }}reports.html">📰 {{ t('Research Reports', '研究报告') }}<span class="d">{{ t('In-depth market & macro reports', '深度市场与宏观报告') }}</span></a>
        <a href="{{ NP }}policy_watch.html">🏛️ {{ t('Fed & Policy Watch', '美联储与政策观察') }}<span class="d">{{ t('Fed + Administration intent & capital rotation', '美联储+政府意图与资本轮动') }}</span></a>
        <a href="{{ NP }}alt_data.html">📊 {{ t('Alternative Data', '替代数据') }}<span class="d">{{ t('Signal intelligence · convergence · influence graph · actionable', '信号情报 · 汇聚 · 影响力图谱 · 可操作') }}</span></a>
        <a href="{{ NP }}congress_trades.html">🏛️ {{ t('Congressional Trades', '国会交易') }}<span class="d">{{ t('Politician disclosures · half-year watchlist · scored to copy', '政界披露 · 半年观察清单 · 评分跟单') }}</span></a>
        <a href="{{ NP }}radar.html">🛰️ {{ t('Divergence Radar', '背离雷达') }}<span class="d">{{ t('Real activity vs price · variant edge', '真实活动 vs 价格 · 变量边际') }}</span></a>
        <a href="{{ NP }}demand.html">🧭 {{ t('Demand Desk', '需求台') }}<span class="d">{{ t('Forward demand vs the priced consensus', '前瞻需求 vs 已定价共识') }}</span></a>
        <a href="{{ NP }}etfs.html">🐳 {{ t('ETF Flows', 'ETF资金流') }}<span class="d">{{ t('Fund flow monitor', '资金流监控') }}</span></a>
        <a href="{{ NP }}anticipation.html">🔮 {{ t('Anticipation', '预判') }}<span class="d">{{ t('Multi-horizon outcome cones', '多周期结果锥') }}</span></a>
        <a href="{{ NP }}transmission.html">🔀 {{ t('Rate & Inflation Transmission', '利率与通胀传导') }}<span class="d">{{ t('How rates & inflation move every asset', '利率与通胀如何驱动各类资产') }}</span></a>
        <a href="{{ NP }}crossasset.html">🧭 {{ t('Cross-Asset', '跨资产') }}<span class="d">{{ t('Correlations · trend · global liquidity', '相关性 · 趋势 · 全球流动性') }}</span></a>
        <a href="{{ NP }}cycle.html">🔄 {{ t('Cycle Intelligence', '周期智慧') }}<span class="d">{{ t('Where every boom-bust cycle sits — history + projection', '每个周期所处位置 · 历史与推演') }}</span></a>
        <a href="{{ NP }}markets.html">🌐 {{ t('Global Market Cycles', '全球市场周期') }}<span class="d">{{ t('Nine national equity markets on one clock', '九大股市同框 · 各自周期位置') }}</span></a>
        <a href="{{ NP }}sector_cycles.html">🧭 {{ t('Sector Cycles', '板块周期') }}<span class="d">{{ t('Every US sector ETF on one cycle clock — phase · turns · the story', '美股各板块同框 · 阶段 · 拐点 · 背后故事') }}</span></a>
        <a href="{{ NP }}signal_lab.html">🔬 {{ t('Signal Lab', '信号实验室') }}<span class="d">{{ t('Consolidated validation scorecard', '验证记分卡汇总') }}</span></a>
        <a href="{{ NP }}factors.html">🧮 {{ t('Factors & Seasonality', '因子 & 季节性') }}<span class="d">{{ t('Value · quality · low-vol · monthly calendar', '价值 · 质量 · 低波动 · 月历') }}</span></a>
      </div>
    </div>
  </div>
  {# Live-price layer (progressive enhancement). Wired here ONCE — this partial is
     the single nav include every built page reaches — so live.js patches any
     .nb-px[data-sym] / .nb-chg[data-sym] across the whole site. No-ops cleanly
     when neither a Worker URL nor a snapshot URL is configured. #}
  <script src="{{ NP }}live_config.js"></script>
  <script src="{{ NP }}live.js"></script>
